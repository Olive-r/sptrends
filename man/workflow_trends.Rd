% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow_trends.R
\name{workflow_trends}
\alias{workflow_trends}
\title{Configure a monotonic or linear trend-analysis workflow}
\usage{
workflow_trends(
  x,
  prewhiten_method = c("TFPW_WS", "TFPW_Y", "TFPW_Z", "VCTFPW", "none"),
  trend_method = c("CMK", "MK", "OLS", "MMK"),
  slope_method = c("TS", "OLS", "RM"),
  fdr_method = c("BKY", "BH", "BY"),
  prewhiten_args = list(),
  trend_args = list(),
  slope_args = list(),
  fdr_args = list(),
  n_cores = 1,
  report = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{x}{A \code{terra::SpatRaster} with one layer per time step,
ordered chronologically. All layers must share the same geometry.
Temporal spacing is assumed regular unless explicit time
coordinates are supplied through the relevant underlying
function's own arguments (e.g. \code{trend_args = list(t = ...)},
\code{slope_args = list(t = ...)}).}

\item{prewhiten_method}{Which prewhitening method to apply before
trend testing, or \code{"none"} to skip this step entirely. One of
\code{"TFPW_WS"} (default; Wang and Swail, 2001), \code{"TFPW_Y"} (Yue,
Pilon and Cavadias, 2002), \code{"TFPW_Z"} (Zhang, Vincent, Hogg and
Niitsoo, 2000), \code{"VCTFPW"} (Wang, Chen, Becker and Liu, 2015), or
\code{"none"}. See \code{\link[=prewhiten]{prewhiten()}}'s own \code{method} argument for the full
comparison between these four, and "Combinations this function
warns about" below for the one combination involving
\code{trend_method} worth knowing about before combining.}

\item{trend_method}{Which trend test to run. One of \code{"CMK"}
(default; Neeti and Eastman, 2011), \code{"MK"} (classic Mann-Kendall),
\code{"OLS"} (tests whether an ordinary-least-squares regression slope
differs from zero, rather than a rank-based statistic -- see
\code{\link[=trend_test]{trend_test()}}'s own \code{"OLS"} entry for the specific \code{p} this
returns and the assumptions its own inference relies on), or
\code{"MMK"} (Hamed and Rao, 1998). See \code{\link[=trend_test]{trend_test()}}'s own \code{method}
argument for the full comparison between these four.}

\item{slope_method}{Which slope estimator to run, or \code{NULL} to skip
this step entirely (matching \code{workflow_tst()}'s own
\code{theil_sen = FALSE}). One of \code{"TS"} (default; Theil-Sen), \code{"OLS"},
\code{"RM"} (Siegel's repeated median), or \code{NULL}. See
\code{\link[=slope_estimator]{slope_estimator()}}'s own \code{method} argument for the full
comparison between the three.}

\item{fdr_method}{Which multiple-testing correction to apply, or
\code{NULL} to skip this step. One of \code{"BKY"} (default; Benjamini,
Krieger and Yekutieli, 2006), \code{"BH"} (Benjamini and Hochberg,
1995), or \code{"BY"} (Benjamini and Yekutieli, 2001). \code{"BY"} is an
opt-in safeguard when control under arbitrary dependence is
scientifically required; it is generally more conservative than
\code{"BH"} and adaptive \code{"BKY"}. See
\code{\link[=fdr_correction]{fdr_correction()}}'s own \code{method} argument, and \code{fdr_by()}'s own
documentation, for the full reasoning.}

\item{prewhiten_args, trend_args, slope_args, fdr_args}{Named lists of
extra arguments forwarded to the corresponding underlying
function, beyond \code{method} itself (which this function's own
arguments above already control) -- e.g.
\code{trend_args = list(ties = TRUE, window_size = 5L)},
\code{fdr_args = list(q = 0.1)}. For CMK, omitting \code{window_size} preserves
the 3 by 3 CMK region described by Neeti and Eastman (2011), as
implemented in TerrSet's Kendall module.
Arguments already controlled directly by \code{workflow_trends()}
itself -- \code{x}, \code{method}, \code{report}, \code{verbose}, and cluster-
management arguments (\code{n_cores}/\code{shared_cluster}) -- must not be
repeated in these lists; a clear error is raised instead of silently
overriding the workflow configuration.}

\item{n_cores}{Integer. Number of workers made available to stages
that support parallel execution -- a single PSOCK cluster is
created once and reused where possible, not one per step. Not
every method actually uses it: \code{slope_method = "OLS"}'s own
vectorised computation and the current \code{"RM"} implementation, for
instance, do not. \code{1} (default): sequential. Requesting more cores
than are actually available falls back to the number detected,
with a message (see \code{?trend_test}'s own \code{n_cores} for the exact
behaviour every step here shares).}

\item{report}{Logical. Forwarded to every step's own \code{report}
argument -- if \code{TRUE}, each analytical stage may print its own
component-level summary and draw its own diagnostic plots as it
runs, not only a single consolidated summary of the whole
workflow at the end. Set \code{FALSE} to suppress this intermediate
output and inspect the returned object directly instead.}

\item{verbose}{Logical. Forwarded to every step's own \code{verbose}
argument, and controls this function's own step-by-step progress
messages and elapsed time. Per-stage times are returned in \code{timing}.}
}
\value{
A list of class
\code{c("workflow_trends", "sptrends")}:
\item{prewhiten}{The output of \code{\link[=prewhiten]{prewhiten()}}, or \code{NULL} if
\code{prewhiten_method = "none"}.}
\item{trend}{The multilayer \code{SpatRaster} returned as \code{trend_test()}'s
own \verb{$stats} (the test statistic, raw \code{p}, and any
method-specific layers -- see \code{?trend_test}'s own "Value"
section for exactly which, since this differs by
\code{trend_method}).}
\item{trend_summary_table}{The output of \code{\link[=trend_summary]{trend_summary()}} on
\code{trend}.}
\item{slope}{The single-layer \code{SpatRaster} extracted from
\code{slope_estimator()}'s own \verb{$slope} (not the full \code{"slope"}
object that function itself returns), or \code{NULL} if
\code{slope_method = NULL}.}
\item{fdr}{The output of \code{\link[=fdr_correction]{fdr_correction()}}, or \code{NULL} if
\code{fdr_method = NULL}.}
\item{timing}{A named list of per-step elapsed seconds, only for
steps that actually ran.}
}
\description{
This function analyses monotonic or linear temporal trends using a
user-selected trend test -- available methods include the
Mann-Kendall family (\code{"CMK"}, \code{"MK"}, \code{"MMK"}) and ordinary
least-squares regression (\code{"OLS"}); it does not detect abrupt
changes, periodicity, or general nonlinear temporal patterns. Like
\code{\link[=workflow_tst]{workflow_tst()}} and \code{\link[=workflow_rta]{workflow_rta()}}, each of which implements one
specific published analytical workflow, this function analyses that
same class of trend. Unlike those two, \code{workflow_trends()} lets
you choose your own combination of prewhitening, trend testing,
slope estimation and multiple-testing correction methods -- useful
when no single published workflow matches what a given analysis
needs, or when comparing how sensitive a result is to that choice
(see the package's own validation framework, \code{\link[=sim_trend_stack]{sim_trend_stack()}}/
\code{\link[=compare_detections]{compare_detections()}}, for a way to evaluate that sensitivity
against data with a known answer, rather than only this function's
own runtime warnings below).
}
\details{
\strong{Function type:} \strong{Core function} -- a configurable composition of the
same analytical stages used by the published workflows.
}
\section{Typical use}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{raster time series
    |
workflow_trends()
    |
optional prewhitening -> trend test -> optional slope -> optional FDR
    |
one `sptrends` result containing every computed stage
}\if{html}{\out{</div>}}

Use this configurable workflow when the fixed published combinations
in \code{\link[=workflow_tst]{workflow_tst()}} and \code{\link[=workflow_rta]{workflow_rta()}} do not match the analysis.
For seasonal input, first pass the \verb{$anomalies} component returned by
\code{\link[=compute_anomalies]{compute_anomalies()}}.
}

\section{Methodological details}{

\strong{Relationship to TST.}
True Significant Trends (TST) is the methodological origin of
sptrends and supplies the architecture of the complete workflow:
temporal preprocessing, trend testing, slope estimation and
multiple-testing correction. TST includes all these stages, but not
every method now available here. \code{workflow_trends()} generalises the
architecture with additional methods and optional stages. A complete
configuration retains the analytical philosophy inherited from TST;
the name TST remains reserved for the published methods and sequence
reproduced by \code{\link[=workflow_tst]{workflow_tst()}}.

Every method available on the individual functions this orchestrates
-- \code{\link[=prewhiten]{prewhiten()}}, \code{\link[=trend_test]{trend_test()}}, \code{\link[=slope_estimator]{slope_estimator()}},
\code{\link[=fdr_correction]{fdr_correction()}} -- can be freely combined here, with two
exceptions this function warns about explicitly rather than
silently allowing (see "Combinations this function warns about"
below). The trend test and slope estimator answer different
questions and need not use the same statistical framework --
mixing a robust trend test with a parametric slope estimator (or
vice versa) is unconventional, but not warned about here.
Nevertheless, users remain responsible for ensuring the selected
combination is appropriate for their own data and scientific
objective; other combinations this function does not itself flag
can still be questionable for a given dataset (e.g. \code{"CMK"} on an
already spatially smoothed series -- see "Combinations this
function warns about" below for the one combination this function
does check for explicitly).

\strong{How it works.}
\enumerate{
\item (optional) \code{prewhiten} -- removes serial autocorrelation.
\item \code{trend_test} -- is there a monotonic trend?
\item (optional) \code{slope_estimator} -- how fast? and
(optional) FDR correction -- which cells survive multiple
testing? These two are independent branches after the trend
test, not a sequence: FDR correction depends only on the test's
own p-values, not on whether a slope was estimated at all.
}
The same ordering logic as the two published workflows applies:
prewhitening (when requested) happens before the trend test, since
the test assumes independent observations; the slope (when
requested) is estimated on the same (optionally prewhitened) series
the test itself used; and FDR correction needs the test's own
p-values to exist first, but nothing else -- it does not depend on
whether a slope was estimated at all. Unlike prewhitening and FDR
correction, which were already optional here from the start, slope
estimation being skippable too (\code{slope_method = NULL}) was added
later, to bring this function to full parity with \code{workflow_tst()}'s
own \code{theil_sen = FALSE} -- see NEWS.md for when.

\strong{Statistical assumptions: \code{alpha} and \code{q}.}
As with \code{workflow_tst()}/\code{workflow_rta()}: \emph{q} (inside \code{fdr_args},
e.g. \code{fdr_args = list(q = 0.1)}) bounds the expected proportion of
false positives among cells called significant \emph{after} correction --
it is not the same quantity as an uncorrected per-cell \eqn{\alpha}.
Applying a per-cell significance threshold across many cells
without multiplicity control can substantially increase the number
of false-positive detections, the multiple-testing problem
\code{fdr_method} exists to address. See \code{?workflow_tst}'s own "A note
on alpha and q" section for the fuller explanation, and
\code{\link[=fdr_correction]{fdr_correction()}} for the distinction in full.

\strong{Statistical assumptions: monotonicity and seasonality.}
As with \code{workflow_tst()}/\code{workflow_rta()}: every method this function
can combine is designed around a \strong{monotonic} trend, not a
periodic/seasonal cycle. If \code{x} has a seasonal cycle (e.g. raw
monthly data with an annual signal), remove it first with
\code{\link[=compute_anomalies]{compute_anomalies()}} and pass the anomalies here, not the raw
seasonal series.

\strong{Computational considerations.} Runtime depends primarily on the
selected methods and raster size. Theil-Sen and repeated-median slopes
are more expensive than OLS; CMK and parallel-capable stages can reuse
one workflow-level PSOCK cluster. Per-stage elapsed times are retained
in the returned object.

\strong{Limitations.} Configurability does not make every combination
scientifically interchangeable. The caller remains responsible for
matching the chosen assumptions to the data; the explicit safeguards
below cover important known conflicts, not every possible misuse.

\strong{Methodological safeguards.} Combining any prewhitening method with
\code{trend_method = "MMK"} is warned against because both address temporal
autocorrelation by different mechanisms. Applying both is redundant and
may compound the correction. The warning is issued before computation;
it does not forcibly stop an explicitly requested analysis.

\strong{Quality assurance.} Integration tests exercise every available method
family, optional stages, invalid and questionable combinations, protected
argument forwarding, BH/BKY/BY display paths, exact warning behaviour,
shared parallel resources, timing fields, and returned S3 structure.
Component results are compared with direct calls to \code{\link[=prewhiten]{prewhiten()}},
\code{\link[=trend_test]{trend_test()}}, \code{\link[=slope_estimator]{slope_estimator()}}, and \code{\link[=fdr_correction]{fdr_correction()}}. See
\code{?sptrends} for the complete internal and external quality strategy.
}

\examples{
\donttest{
# Annual mean NDVI from the bundled environmental dataset -- reduced
# to a small window around a cell confirmed to have a complete
# series (found here programmatically, rather than assumed by
# coordinates, which risks landing on a region with no valid
# coverage at all) purely to keep this example fast; the full
# dataset works identically, just with more cells.
r_full <- read_ordered_stack(example_data("vhp_ndvi"))
ok <- stats::complete.cases(terra::values(r_full, mat = TRUE))
rc <- terra::rowColFromCell(r_full, which(ok)[1])
row_lo <- max(1, rc[1] - 10)
row_hi <- min(terra::nrow(r_full), rc[1] + 10)
col_lo <- max(1, rc[2] - 10)
col_hi <- min(terra::ncol(r_full), rc[2] + 10)
r <- terra::crop(r_full, terra::ext(
  terra::xFromCol(r_full, col_lo), terra::xFromCol(r_full, col_hi),
  terra::yFromRow(r_full, row_hi), terra::yFromRow(r_full, row_lo)))

# A combination neither TST nor RTA offers on its own: Yue-Pilon
# prewhitening, classic Mann-Kendall, OLS slope, standard (BH) FDR.
result <- workflow_trends(r, prewhiten_method = "TFPW_Y",
                            trend_method = "MK", slope_method = "OLS",
                            fdr_method = "BH",
                            report = FALSE, verbose = FALSE)
result
plot(result, which = "significance")
plot(result, which = "slope")

# Siegel's repeated median as the slope estimator instead of OLS --
# useful when a dataset is suspected to have enough outliers or
# leverage points that even Theil-Sen's own ~29\% breakdown point
# might not fully resist them (see ?slope_estimator's own "RM" entry
# under `method`).
result_rm <- workflow_trends(r, prewhiten_method = "TFPW_Y",
                               trend_method = "MK", slope_method = "RM",
                               fdr_method = "BH",
                               report = FALSE, verbose = FALSE)
plot(result_rm, which = "slope")

# Skipping optional stages entirely -- slope_method/fdr_method both
# NULL leaves $slope/$fdr NULL too, rather than some placeholder
# value, making the modularity concrete rather than just described.
result_test_only <- workflow_trends(r, prewhiten_method = "none",
                                      trend_method = "MMK",
                                      slope_method = NULL,
                                      fdr_method = NULL,
                                      report = FALSE, verbose = FALSE)
is.null(result_test_only$slope)
is.null(result_test_only$fdr)
}

\dontrun{
# A combination this function warns about: MMK plus prewhitening.
# Not run automatically (unlike the examples above) specifically
# because its only purpose here is to demonstrate the warning
# itself, which would otherwise fire on every R CMD check and every
# example() call.
r <- read_ordered_stack(example_data("vhp_ndvi"))
result_warns <- workflow_trends(r, trend_method = "MMK",
                                  report = FALSE, verbose = FALSE)
}

}
\references{
Primary method reference (\code{prewhiten_method = "TFPW_WS"}, the
default):
\itemize{
\item Wang, X.L. and Swail, V.R. (2001) Changes of Extreme Wave Heights
in Northern Hemisphere Oceans and Related Atmospheric Circulation
Regimes. Journal of Climate, 14(10), 2204-2221.
}

Primary method reference (\code{prewhiten_method = "TFPW_Y"}):
\itemize{
\item Yue, S., Pilon, P., Phinney, B. and Cavadias, G. (2002) The
influence of autocorrelation on the ability to detect trend in
hydrological series. Hydrological Processes, 16(9), 1807-1829.
\doi{10.1002/hyp.1095}
}

Primary method reference (\code{prewhiten_method = "TFPW_Z"}):
\itemize{
\item Zhang, X., Vincent, L.A., Hogg, W.D. and Niitsoo, A. (2000)
Temperature and precipitation trends in Canada during the 20th
century. Atmosphere-Ocean, 38(3), 395-429.
\doi{10.1080/07055900.2000.9649654}
}

Primary method reference (\code{prewhiten_method = "VCTFPW"}):
\itemize{
\item Wang, W., Chen, Y., Becker, S. and Liu, B. (2015) Variance
Correction Prewhitening Method for Trend Detection in
Autocorrelated Data. Journal of Hydrologic Engineering, 20(12),
04015033. \doi{10.1061/(ASCE)HE.1943-5584.0001234}
}

Primary method reference (\code{trend_method = "CMK"}, the default):
\itemize{
\item Neeti, N. and Eastman, J.R. (2011) A Contextual Mann-Kendall
Approach for the Assessment of Trend Significance in Image Time
Series. Transactions in GIS, 15(5), 599-611.
\doi{10.1111/j.1467-9671.2011.01280.x}
}

Primary method reference (\code{trend_method = "MMK"}):
\itemize{
\item Hamed, K.H. and Rao, A.R. (1998) A modified Mann-Kendall trend
test for autocorrelated data. Journal of Hydrology, 204(1-4),
182-196. \doi{10.1016/S0022-1694(97)00125-X}
}

Primary method reference (\code{fdr_method = "BH"}):
\itemize{
\item Benjamini, Y. and Hochberg, Y. (1995) Controlling the False
Discovery Rate: A Practical and Powerful Approach to Multiple
Testing. Journal of the Royal Statistical Society: Series B, 57,
289-300. \doi{10.1111/j.2517-6161.1995.tb02031.x}
}

Primary method reference (\code{fdr_method = "BKY"}, the default):
\itemize{
\item Benjamini, Y., Krieger, A.M. and Yekutieli, D. (2006) Adaptive
Linear Step-Up Procedures that Control the False Discovery Rate.
Biometrika, 93(3), 491-507. \doi{10.1093/biomet/93.3.491}
}

Primary method reference (\code{fdr_method = "BY"}, not recommended by
default -- see the \code{fdr_method} argument above for why):
\itemize{
\item Benjamini, Y. and Yekutieli, D. (2001) The control of the false
discovery rate in multiple testing under dependency. Annals of
Statistics, 29(4), 1165-1188. \doi{10.1214/aos/1013699998}
}

\code{trend_method = "MK"}, \code{trend_method = "OLS"}, and
\code{slope_method = "TS"}/\code{"OLS"}/\code{"RM"} do have their own foundational
references (Mann, 1945 and Kendall, 1975; Legendre, 1805 and Gauss,
1809; Theil, 1950 and Sen, 1968; Siegel, 1982, respectively) --
cited in full under \code{?trend_test} and \code{?slope_estimator} rather than
repeated here.
}
\seealso{
Other pipeline functions:
\code{\link[=workflow_rta]{workflow_rta()}},
\code{\link[=workflow_tst]{workflow_tst()}}
}
\concept{pipeline functions}
