% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fdr.R
\name{fdr_by}
\alias{fdr_by}
\title{Benjamini-Yekutieli (2001) false discovery rate correction}
\usage{
fdr_by(p, q = 0.05)
}
\arguments{
\item{p}{Numeric vector of raw p-values in \verb{[0, 1]} (may contain \code{NA}).}

\item{q}{Numeric. Target FDR level -- see \code{\link[=fdr_bh]{fdr_bh()}}'s own \code{q}
documentation for the distinction between this and a per-test
\code{alpha}, which applies identically here.}
}
\value{
A list with \code{q_value} (BY-adjusted p-values) and \code{reject}
(logical).
}
\description{
Thin wrapper around \code{\link[stats:p.adjust]{stats::p.adjust()}} with \code{method = "BY"} -- base R
already implements this correctly, so it is not reimplemented.
}
\details{
\strong{Function type:} \strong{Support function} -- computes the BY safeguard
used internally by \code{\link[=fdr_correction]{fdr_correction()}}. Exported as a standalone
convenience for callers who only need a plain vector of p-values
corrected; call \code{fdr_correction(p, method = "BY")} instead for the
full result.
}
\section{Typical use}{

Supply one family of raw p-values to \code{\link[=fdr_correction]{fdr_correction()}} with
\code{method = "BY"} when arbitrary dependence is a material concern, or
compare it with BH as a sensitivity analysis.
}

\section{Methodological details}{

\strong{Methods and method selection}

\code{\link[=fdr_bh]{fdr_bh()}} controls FDR under independence and specified forms of
positive dependence. A positive Moran statistic can be compatible
with that setting, but it does not prove the formal PRDS condition.
BY remains valid under arbitrary dependence, at the cost of being
more conservative (usually fewer rejected hypotheses) than BH for
the same data. It is therefore available as an explicit safeguard
when arbitrary dependence is a scientifically material concern, not
as the package default. Agreement between BH and BY is a useful
sensitivity result, but it does not by itself establish BH's
dependence assumptions.

\strong{Statistical assumptions and limitations}

BY controls FDR without requiring the independence or PRDS conditions
used by BH. Its harmonic correction can be substantially conservative,
so this broader guarantee may considerably reduce power.

\strong{Quality assurance}

Adjusted values are generated directly by \code{stats::p.adjust()} and are
also checked through the package's automated FDR tests.
}

\examples{
# The same five p-values fdr_bh()'s own example uses -- compare the
# two directly on identical data.
fdr_by(c(0.001, 0.01, 0.02, 0.5, 0.8))
}
\references{
Primary method reference:
\itemize{
\item Benjamini, Y., & Yekutieli, D. (2001). The control of the false
discovery rate in multiple testing under dependency. Annals of
Statistics, 29(4), 1165-1188. \doi{10.1214/aos/1013699998}
}
}
\seealso{
Other FDR correction functions:
\code{\link[=fdr_bh]{fdr_bh()}},
\code{\link[=fdr_bky]{fdr_bky()}},
\code{\link[=fdr_comparison_barplot]{fdr_comparison_barplot()}},
\code{\link[=fdr_correction]{fdr_correction()}},
\code{\link[=fdr_direction_plot]{fdr_direction_plot()}},
\code{\link[=fdr_direction_summary]{fdr_direction_summary()}},
\code{\link[=fdr_pvalue_histogram]{fdr_pvalue_histogram()}},
\code{\link[=fdr_significance_maps]{fdr_significance_maps()}},
\code{\link[=fdr_summary]{fdr_summary()}},
\code{\link[=fdr_threshold_plot]{fdr_threshold_plot()}}
}
\concept{FDR correction functions}
