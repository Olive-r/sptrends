% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend_test.R
\name{trend_maps}
\alias{trend_maps}
\title{Maps of CMK trend results}
\usage{
trend_maps(
  trend,
  alpha = 0.05,
  panels = c("statistic", "pvalue", "significance", "direction"),
  path = NULL
)
}
\arguments{
\item{trend}{The \verb{$stats} field of \code{\link[=trend_test]{trend_test()}}'s output.}

\item{alpha}{Numeric. Significance threshold used for the significance
and direction-of-change maps. \strong{Uncorrected} for multiple testing
across cells -- these maps are diagnostic, not a final significance
result; run \code{\link[=fdr_correction]{fdr_correction()}} on \code{trend$p} (and consider
\code{\link[=direction_map]{direction_map()}} instead) before reporting which cells are
significant.}

\item{panels}{Character vector: which of the 4 available maps to draw,
any subset of \code{c("statistic", "pvalue", "significance", "direction")} (default: all four, in that order). \code{"statistic"}:
the raw \code{Sm}/\code{S} trend statistic (diverging). \code{"pvalue"}: the
uncorrected p-value (sequential). \code{"significance"}: which cells
cross \code{alpha} (binary). \code{"direction"}: increase/decrease/no-change
among the significant cells. Reachable via \code{plot(x, panels = ...)}
on a \code{\link[=trend_test]{trend_test()}} result, which forwards this argument through.}

\item{path}{Character or \code{NULL}. If supplied, PNGs are written using
this as a filename prefix.}
}
\value{
Invisibly, a list with \code{significant} and \code{classes} (both
\code{SpatRaster}, \code{NULL} if \code{"significance"}/\code{"direction"} respectively
was not in \code{panels}).
}
\description{
\strong{Function type:} \strong{Reporting/derived function} -- summarises or
plots the output of
another function; it does not compute any new statistic. Not
exported -- called internally by \code{report = TRUE}, and reachable
from outside the package via \code{plot()}.
}
\examples{
r <- read_ordered_stack(example_data("vhp_ndvi"))
trend <- trend_test(r, report = FALSE, verbose = FALSE)

# All four maps: the trend statistic, the (uncorrected) p-value, which
# cells cross alpha = 0.05, and the direction of change among those.
plot(trend)

# Just the two most commonly wanted together -- raw/binary
# significance and direction -- skipping the statistic and p-value
# panels, whose raw scale is harder to read at a glance than a
# simple yes/no or increase/decrease map.
plot(trend, panels = c("significance", "direction"))

}
\references{
\itemize{
\item Neeti, N. and Eastman, J.R. (2011) A Contextual Mann-Kendall Approach
for the Assessment of Trend Significance in Image Time Series.
Transactions in GIS, 15(5), 599-611. \doi{10.1111/j.1467-9671.2011.01280.x}
}
}
\seealso{
Other Contextual Mann-Kendall functions:
\code{\link[=prepare_cmk_neighbourhood]{prepare_cmk_neighbourhood()}},
\code{\link[=trend_histograms]{trend_histograms()}},
\code{\link[=trend_summary]{trend_summary()}}
}
\concept{Contextual Mann-Kendall functions}
\keyword{internal}
