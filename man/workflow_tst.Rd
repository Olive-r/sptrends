% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow_tst.R
\name{workflow_tst}
\alias{workflow_tst}
\title{True Significant Trends (TST): the full pipeline in one call}
\usage{
workflow_tst(
  x,
  prewhiten = TRUE,
  prewhiten_args = list(),
  export_dw = FALSE,
  cmk_args = list(),
  theil_sen = TRUE,
  theil_sen_args = list(),
  alpha = 0.05,
  moran_check = FALSE,
  fdr_method = c("BKY", "BH", "BY"),
  q = 0.05,
  bky_implementation = c("multtest", "original"),
  report = TRUE,
  verbose = TRUE,
  n_cores = 1
)
}
\arguments{
\item{x}{A \code{terra::SpatRaster}; each layer is one time step, in
increasing chronological order.}

\item{prewhiten}{\strong{(Preprocessing)} Logical. If \code{TRUE} (default), run
\code{\link[=prewhiten]{prewhiten()}} first. If \code{FALSE}, \code{x} is supplied to the trend
test unmodified.}

\item{prewhiten_args}{A named list of extra arguments forwarded to
\code{\link[=prewhiten]{prewhiten()}} (e.g. \code{list(dw_method = "test")}). Workflow-managed
transport and reporting arguments cannot be overridden here.

\strong{Known limitation with irregular time spacing and \code{TFPW_Y}:} this
workflow has no top-level \code{t} argument of its own -- if your
observation times are irregularly spaced (not one evenly-spaced
step per layer) and you need a genuine time axis for
\code{\link[=trend_test]{trend_test()}}/\code{\link[=slope_estimator]{slope_estimator()}} rather than the default
\code{1:n} layer index, you must supply the correct \code{t} yourself in
\code{prewhiten_args}, \code{cmk_args} and \code{theil_sen_args} separately, and
keep them consistent by hand. \code{TFPW_Y} specifically drops the
first observation (see \code{\link[=prewhiten]{prewhiten()}}'s own "Value" section), so
the \code{t} you pass to \code{cmk_args}/\code{theil_sen_args} after using
\code{TFPW_Y} must itself have its own first element dropped to stay
aligned with the one-layer-shorter prewhitened series -- this
workflow does not do that adjustment for you. Not an issue for
\code{TFPW_WS} (default), which keeps every layer.}

\item{export_dw}{Logical. If \code{TRUE}, include the Durbin-Watson
prewhitening diagnostics (\code{diagnostics} from \code{\link[=prewhiten]{prewhiten()}})
in the return value, even though the trend test itself does not need
them. Ignored if \code{prewhiten = FALSE}. Default \code{FALSE}.}

\item{cmk_args}{\strong{(Trend detection)} A named list of extra arguments
forwarded to
\code{\link[=trend_test]{trend_test()}} (e.g. \code{list(window_size = 5L)} for a broader CMK
region, or \code{list(method = "MK", n_cores = 4)}). The default empty
list preserves the 3 by 3 CMK region described by Neeti and Eastman
(2011), as implemented in TerrSet's Kendall module;
changing it creates a TST-inspired variant rather than an exact
reproduction of the published workflow. See \code{prewhiten_args}
above for a known limitation with irregular time spacing and
\code{TFPW_Y} if you pass \code{t} here.}

\item{theil_sen}{\strong{(Slope estimation)} Logical. If \code{TRUE} (default),
also compute the
Theil-Sen slope (magnitude of change) via \code{\link[=slope_estimator]{slope_estimator()}} --
see "Computational considerations" above before leaving
this on for long time series.}

\item{theil_sen_args}{A named list of extra arguments forwarded to
\code{\link[=slope_estimator]{slope_estimator()}} (e.g. \code{list(max_pairs = 20000, n_cores = 4)}).
Ignored if \code{theil_sen = FALSE}. \code{smooth_neighbourhood} stays at
\code{\link[=slope_estimator]{slope_estimator()}}'s own default (\code{FALSE}) unless set here (e.g.
\code{list(smooth_neighbourhood = TRUE)}) -- see "Theil-Sen has a real
computational cost" above for why. See \code{prewhiten_args} above for
a known limitation with irregular time spacing and \code{TFPW_Y} if
you pass \code{t} here.}

\item{alpha}{Numeric vector of significance threshold(s) used for
reporting the (uncorrected) trend result -- supplied to \code{\link[=trend_summary]{trend_summary()}}
as \code{alpha}, and used as the single threshold for \code{\link[=trend_maps]{trend_maps()}}:
\code{0.05} if it is one of the values in \code{alpha}, otherwise the
strictest (smallest) value supplied. Defaults to a single \code{0.05},
matching \code{q}'s own default -- a normal analysis picks one alpha
and one q, not several at once. Pass a vector explicitly (e.g.
\code{alpha = c(0.1, 0.05, 0.01)}) to compare several thresholds side
by side in \code{trend_summary_table} instead. See "Statistical
assumptions: \code{alpha} and \code{q}" above.}

\item{moran_check}{\strong{(Multiple testing)} Logical. Forwarded to
\code{\link[=fdr_correction]{fdr_correction()}} as-is --
if \code{TRUE}, runs \code{\link[=spatial_autocorrelation]{spatial_autocorrelation()}} on the trend p-value
raster as part of the FDR step. Default \code{FALSE} (Moran's I stays a
separate, deliberate diagnostic -- see the package vignette).}

\item{fdr_method}{Character vector supplied as \code{method} to
\code{\link[=fdr_correction]{fdr_correction()}}. The Usage displays all supported values:
\code{"BKY"}, \code{"BH"} and \code{"BY"}; when omitted, \code{"BKY"} remains the
default originally used
in the TST methodology (Gutiérrez-Hernández & García, 2025): it
adapts to the estimated proportion of true nulls, gaining
statistical power over BH while targeting false-discovery-rate
control. Supply one method or a vector of methods; BY is the
conservative option for arbitrary dependence. Set to \code{NULL} to
skip FDR correction entirely.}

\item{q}{Numeric. Target FDR level, forwarded to \code{\link[=fdr_correction]{fdr_correction()}} --
not an adjusted or renamed \code{alpha}: it limits the expected false
discovery proportion among rejected hypotheses. See \code{\link[=fdr_correction]{fdr_correction()}}
and "Statistical assumptions: \code{alpha} and \code{q}" above.}

\item{bky_implementation}{\code{"multtest"} (default, unchanged from previous
versions) or \code{"original"}, forwarded to \code{\link[=fdr_bky]{fdr_bky()}} via
\code{\link[=fdr_correction]{fdr_correction()}} -- see \code{\link[=fdr_bky]{fdr_bky()}}'s own documentation for the
difference between the two. Ignored if \code{fdr_method} does not
include \code{"BKY"}.}

\item{report}{\strong{(Reporting)} Logical. If \code{TRUE} (default), each step
prints its own
summary table and draws its diagnostic plots as it runs (same effect
as calling each function directly with \code{report = TRUE}). Set to
\code{FALSE} for silent, plot-free programmatic use.}

\item{verbose}{Logical. Print progress messages and elapsed time for
the complete workflow. Per-stage times are also returned in \code{timing}.}

\item{n_cores}{Integer. \code{1} (default): every step runs sequentially.
\verb{> 1}: builds \strong{one} \code{parallel::makeCluster()} PSOCK cluster,
shared across every parallel step below (currently the CMK and
Theil-Sen steps) instead of each step creating and tearing down
its own -- avoiding the repeated process-spawn overhead of doing
that two or three times in a row for what is, from the caller's
own perspective, one parallel request. The cluster is closed
automatically when \code{workflow_tst()} returns. Setting \code{n_cores}
inside \code{cmk_args}/\code{theil_sen_args} directly still works exactly as
before (each step falls back to building its own cluster from
that value) -- but only when this top-level \code{n_cores} is left at
its default of \code{1}; when both are set, this one wins and the
per-step \code{n_cores} inside \code{cmk_args}/\code{theil_sen_args} is ignored,
since a single shared cluster and per-step separate ones cannot
both apply to the same call.}
}
\value{
An object of class \code{c("tst", "sptrends")} (the second, shared
with \code{\link[=workflow_rta]{workflow_rta()}}'s own return value, is for \code{print()}/\code{summary()}/
\code{plot()} -- see \code{\link[=print.sptrends]{print.sptrends()}}): a list
with
\item{prewhiten}{The full output of \code{\link[=prewhiten]{prewhiten()}}, or
\code{NULL} if \code{prewhiten = FALSE}.}
\item{dw_diagnostics}{The prewhitening \code{diagnostics} raster, only if
\code{export_dw = TRUE}.}
\item{trend}{The \verb{$stats} field of \code{\link[=trend_test]{trend_test()}}'s own output (a
\code{SpatRaster} with the trend statistics themselves) -- not the
full \code{\link[=trend_test]{trend_test()}} return value, which also includes
\code{neighbourhood}/\code{window_size}.}
\item{trend_summary_table}{The output of \code{\link[=trend_summary]{trend_summary()}} (invisible
data frame).}
\item{theil_sen}{The \verb{$slope} field of \code{\link[=slope_estimator]{slope_estimator()}}'s own
output (a \code{SpatRaster}), or \code{NULL} if \code{theil_sen = FALSE}. Not
the full \code{\link[=slope_estimator]{slope_estimator()}} return value. Not smoothed by
default (see the \code{theil_sen_args} note above) -- check
\code{theil_sen_smoothed} if you need to know whether it was (e.g.
because you set it yourself).}
\item{theil_sen_smoothed}{Logical: whether \code{theil_sen} was computed
with \code{smooth_neighbourhood = TRUE}. \code{FALSE} by default; \code{NULL} if
\code{theil_sen = FALSE}.}
\item{fdr}{The output of \code{\link[=fdr_correction]{fdr_correction()}}, or \code{NULL} if
\code{fdr_method = NULL}.}
\item{timing}{A named list of elapsed seconds per step actually run
(\code{prewhiten}, \code{CMK}, \code{theil_sen}, \code{fdr} -- only the ones that ran
are present), each measured with \code{Sys.time()} around that step's
own function call. Coarse (whole-step, not line-by-line) and not a
substitute for a proper profiler, but enough for noticing which
step dominates on your own data.}

Use \code{print()} for a one-line summary, \code{summary()} for the full detail,
and \code{plot()} for a map -- see \code{\link[=plot.sptrends]{plot.sptrends()}}.
}
\description{
Implements the complete workflow for robust statistical inference of
monotonic trends in gridded raster time series. The main entry point
of sptrends: chains \code{\link[=prewhiten]{prewhiten()}}, \code{\link[=trend_test]{trend_test()}}, \code{\link[=slope_estimator]{slope_estimator()}},
and \code{\link[=fdr_correction]{fdr_correction()}}, in that order, into the True Significant
Trends (TST) workflow. Each step is optional and all underlying
parameters remain available directly on the individual functions --
\code{workflow_tst()} does not replace them, it saves wiring the calls
together for the common case, and returns a single \code{"tst"} object
with its own \code{print()}, \code{summary()}, and \code{plot()} methods.
}
\details{
Unlike many trend-analysis workflows, TST separates preprocessing,
hypothesis testing, effect-size estimation, and multiple-testing
correction into independent but composable steps.
\code{workflow_tst()} simply orchestrates those steps into a reproducible
pipeline without hiding any of their parameters.

Moran's I (\code{\link[=spatial_autocorrelation]{spatial_autocorrelation()}}) is \strong{not} part of this pipeline:
it is an independent, general spatial diagnostic. Its optional use on
inferential fields can reveal dependence relevant to FDR interpretation,
but cannot verify all assumptions of an FDR procedure -- see the package
vignette.

\strong{Function type:} \strong{Core function} -- the complete published TST workflow.
}
\section{Typical use}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{raster time series
    |
workflow_tst()
    |
selective prewhitening -> CMK -> Theil-Sen -> adaptive FDR
    |
one `tst` result containing every stage
}\if{html}{\out{</div>}}

For seasonal input, first use \code{\link[=compute_anomalies]{compute_anomalies()}} and pass its
\code{anomalies} raster. For long series, consider setting \code{max_pairs}
through \code{theil_sen_args}; see "Computational considerations" below.
}

\section{Methodological details}{

\strong{How it works.}

\if{html}{\out{<div class="sourceCode">}}\preformatted{Input raster
    |
(optional) prewhiten          -- removes serial autocorrelation
    |
trend_test                    -- is there a monotonic trend? (CMK)
    |
(optional) slope_estimator    -- how fast? (Theil-Sen or OLS)
    |
(optional) FDR correction     -- which cells survive multiple testing?
    |
"tst" object
}\if{html}{\out{</div>}}

The four steps run in this fixed order because each one's own
assumptions depend on what came before it: prewhitening needs to
happen before the trend test, since the test assumes independent
observations; the slope is estimated on the same (optionally
prewhitened) series the test itself used, so the two describe the
same data; and FDR correction needs the test's own p-values to exist
first. Prewhitening, slope estimation, and FDR correction are each
individually optional -- not every analysis needs all three (a
quick exploratory look at significance alone might skip slope and
FDR entirely; data already known to have negligible serial
correlation might skip prewhitening) -- but the trend test itself is
not optional, since every other step either feeds into it or
consumes its output. The returned object keeps every intermediate
result it computed (see "Value" below), not only the final
one, so that any step's own output can be inspected or reused
without recomputing the whole pipeline.

\strong{Statistical assumptions: \code{alpha} and \code{q}.}
This is one of the most common mistakes in gridded trend analysis, so
it is worth spelling out: \code{alpha} (e.g. \code{0.05}) is defined for a
\emph{single} hypothesis test. A raster is not one test -- it is one test
per cell, potentially thousands of them run simultaneously. Applying
\code{alpha} cell by cell, as if each cell were the only test being run, is
exactly the multiple-testing error this package's FDR step exists to
fix (see the "Warning" section of \code{?trend_test}); it is
not a harmless simplification.

It is common practice to set \code{q} equal to \code{alpha} (e.g. both \code{0.05}) for
convenience and comparability between the uncorrected and corrected
results reported here -- this function does not enforce that, \code{alpha}
and \code{q} are independent arguments. But equal values does not mean equal
meaning: \code{alpha} bounds the error rate of each individual cell's test,
while \code{q} bounds the expected proportion of false positives among all
the cells called significant after correction (see \code{\link[=fdr_correction]{fdr_correction()}} for the
full distinction). Do not read \code{trend_summary_table} (based on \code{alpha},
uncorrected) and the FDR results (based on \code{q}) as answering the same
question just because the numbers match. Unlike \code{alpha}, which this
function reports at three conventional levels for context (see
\code{alpha} below), \code{q = 0.05} has little reason to change: it is the
standard target FDR level in the literature this package builds on,
and lowering it (e.g. to \code{0.01}) mainly costs statistical power rather
than offering a meaningfully different guarantee.

\strong{Statistical assumptions: monotonicity and seasonality.}
Every step here (prewhitening, the Contextual Mann-Kendall test,
Theil-Sen) is designed around a \strong{monotonic} trend -- a consistent
tendency to increase or decrease -- not a periodic/seasonal cycle. If
\code{x} has a seasonal cycle (e.g. raw monthly data with an annual signal),
remove it first with \code{\link[=compute_anomalies]{compute_anomalies()}} and pass the anomalies to
\code{workflow_tst()}, not the raw seasonal series.

\strong{Computational considerations.}
Unlike the Mann-Kendall S statistic, the Theil-Sen slope needs every
pairwise slope in the series (\code{n*(n-1)/2} per cell) to take their
median -- it cannot be accumulated as a running sum, so it does not
scale the same way. For short series (a few dozen time steps) this is
unnoticeable; for long series (hundreds to thousands of steps, e.g.
multi-decade monthly data) it can become the slowest step in the whole
workflow. \code{theil_sen = TRUE} by default, to match the published TST
workflow, but for long series consider \code{theil_sen = FALSE}, or tune
\code{theil_sen_args} (\code{max_pairs} to subsample pairs, \code{n_cores} to
parallelise) -- see \code{\link[=slope_estimator]{slope_estimator()}}. \code{smooth_neighbourhood} stays
at \code{\link[=slope_estimator]{slope_estimator()}}'s own default (\code{FALSE}) unless you set it
yourself via \code{theil_sen_args = list(smooth_neighbourhood = TRUE)}:
that mechanism is not part of the published TST method (neither TST
nor RTA originally included any such smoothing -- it is this
package's own optional addition on top of both), so there is no
principled reason for \code{workflow_tst()} and \code{\link[=workflow_rta]{workflow_rta()}} to
default to different
values for it -- see \code{?slope_estimator}'s "Optional queen-neighbourhood
smoothing" section for exactly what it does and why it is
off by default.

\strong{Limitations.} The workflow targets monotonic trends and does not
detect abrupt breaks, periodicity, or general nonlinear change. Optional
stages permit exploratory variants, but only the complete default
configuration reproduces the published TST workflow.

\strong{Quality assurance.} Each component is validated independently in its
own function. Workflow-level tests additionally verify stage order,
optional prewhitening, argument forwarding, shared-cluster behaviour,
sequential/parallel equivalence, timings, S3 structure, summaries,
plots, and propagation of raw and FDR-corrected results. See \code{?sptrends}
for the internal release protocol and external numerical controls.
}

\examples{
\donttest{
# Annual mean NDVI from the bundled environmental dataset.
r <- read_ordered_stack(example_data("vhp_ndvi"))

# Run the full workflow: prewhiten -> Contextual Mann-Kendall ->
# Theil-Sen -> FDR-BKY (only BKY, not BH -- see the "fdr_method"
# argument below for why).
result <- workflow_tst(r, report = FALSE, verbose = FALSE)

# A "tst" object: printing it gives a one-line-per-step summary
# (cells modified by prewhitening, the trend test's cell count, the
# Theil-Sen slope range, and how many cells are significant after
# FDR-BKY correction).
result

# Three reports worth seeing: how much of the map is significant at
# all, how fast the significant cells are changing, and which way.
plot(result, which = "significance")
plot(result, which = "slope")
plot(result)
}

}
\references{
Source of this workflow (primary reference -- this pipeline is a
direct implementation of it):
\itemize{
\item Gutiérrez-Hernández, O. and García, L.V. (2025) Uncovering true
significant trends in global greening. Remote Sensing Applications:
Society and Environment, 37, 101377. \doi{10.1016/j.rsase.2024.101377}
}

Step 1, selective AR(1) prewhitening (see \code{\link[=prewhiten]{prewhiten()}} for
the full reference list, including the Durbin-Watson gate):
\itemize{
\item Wang, X.L. and Swail, V.R. (2001) Changes of Extreme Wave Heights in
Northern Hemisphere Oceans and Related Atmospheric Circulation
Regimes. Journal of Climate, 14(10), 2204-2221.
}

Step 2, Contextual Mann-Kendall (see \code{\link[=trend_test]{trend_test()}} for
the full reference list, including the foundational Mann-Kendall
statistic it builds on):
\itemize{
\item Neeti, N. and Eastman, J.R. (2011) A Contextual Mann-Kendall Approach
for the Assessment of Trend Significance in Image Time Series.
Transactions in GIS, 15(5), 599-611. \doi{10.1111/j.1467-9671.2011.01280.x}
}

Step 3, Theil-Sen slope (see \code{\link[=slope_estimator]{slope_estimator()}} for the full
reference list):
\itemize{
\item Theil, H. (1950) A rank-invariant method of linear and polynomial
regression analysis. Indagationes Mathematicae, 12, 85-91 (Part I;
published in three parts). No DOI available (pre-DOI-era publication).
\item Sen, P.K. (1968) Estimates of the regression coefficient based on
Kendall's tau. Journal of the American Statistical Association, 63,
1379-1389. \doi{10.1080/01621459.1968.10480934}
}

Step 4, FDR correction -- BKY (default) and BH (see \code{\link[=fdr_bky]{fdr_bky()}} and
\code{\link[=fdr_correction]{fdr_correction()}} for the full reference lists):
\itemize{
\item Benjamini, Y., & Hochberg, Y. (1995) Controlling the False Discovery
Rate: A Practical and Powerful Approach to Multiple Testing. Journal
of the Royal Statistical Society: Series B, 57, 289-300.
\doi{10.1111/j.2517-6161.1995.tb02031.x}
\item Benjamini, Y., Krieger, A. M., & Yekutieli, D. (2006) Adaptive
Linear Step-Up Procedures that Control the False Discovery Rate.
Biometrika, 93(3), 491-507. \doi{10.1093/biomet/93.3.491}
}
}
\seealso{
Other pipeline functions:
\code{\link[=workflow_rta]{workflow_rta()}},
\code{\link[=workflow_trends]{workflow_trends()}}
}
\concept{pipeline functions}
