% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_data.R
\name{sim_trend_stack}
\alias{sim_trend_stack}
\title{Generate a synthetic gridded time series with known true trends}
\usage{
sim_trend_stack(
  nrow = 15,
  ncol = 15,
  n_time = 10,
  trend_strength = 0.15,
  trend_shape = c("radial", "gradient", "block", "square", "rectangle", "ellipse",
    "custom"),
  trend_fraction = 0.9,
  ar1 = 0.3,
  noise_sd = 1,
  noise_dist = c("gaussian", "t"),
  t_df = 4,
  smooth_radius = 1L,
  spatial_rho = 1,
  spatial_model = c("legacy", "independent", "gaussian", "exponential", "matern"),
  spatial_range = 1,
  spatial_smoothness = 0.5,
  signal_size = NULL,
  signal_location = c("centre", "random"),
  signal_angle = 0,
  signal_axis_ratio = 1,
  custom_mask = NULL,
  constant_block = TRUE,
  break_type = c("none", "mean", "slope"),
  break_time = NULL,
  break_fraction = 0.3,
  break_magnitude = 2,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{nrow, ncol}{Integer. Raster dimensions in cells.}

\item{n_time}{Integer. Number of time steps (layers).}

\item{trend_strength}{Numeric. Maximum magnitude of the true slope (see
"How the trend is generated -- trend field" above for how it
varies spatially according to \code{trend_shape}).}

\item{trend_shape}{Spatial pattern of the true slope field: \code{"radial"},
\code{"gradient"}, \code{"block"}, \code{"square"}, \code{"rectangle"}, \code{"ellipse"},
or \code{"custom"}; see "How the trend is generated -- trend field" above.}

\item{trend_fraction}{Numeric in \verb{[0, 1]}. Proportion of cells that
keep a non-zero true slope; the rest are forced to exactly \code{0}.
Default \code{0.9}. Set to \code{0} for a complete null field (no trend
anywhere), or \code{1} for a trend at every cell \code{trend_shape} assigns one
to.}

\item{ar1}{Numeric in \verb{(-1, 1)}. AR(1) autocorrelation coefficient of the
noise term across time, applied independently within each cell.}

\item{noise_sd}{Numeric \verb{> 0}. Standard deviation of the noise term --
the main control over how easy or hard the true trend is to detect
against the background noise (signal-to-noise ratio). Default \code{1}.}

\item{noise_dist}{\code{"gaussian"} (default) or \code{"t"} -- the distribution
of the noise term, always rescaled to the standard deviation
\code{noise_sd} regardless of shape. This matters for comparing methods:
Mann-Kendall-based tests (and Theil-Sen) are rank-based and robust to
heavy-tailed, outlier-prone noise, while a parametric method like OLS
is the more \emph{efficient} choice specifically when noise is Gaussian --
the classic case for preferring a non-parametric method only shows up
with non-Gaussian noise. \code{"t"} draws from a Student's t distribution
with \code{t_df} degrees of freedom instead, giving heavier tails (more
extreme outliers) at the same standard deviation.}

\item{t_df}{Numeric \verb{> 2}. Degrees of freedom for the noise
distribution when \code{noise_dist = "t"}; ignored otherwise. Lower values
give heavier tails (more frequent/extreme outliers); values just
above \code{2} are extreme, values above \code{30} are practically
indistinguishable from Gaussian. Must exceed \code{2} for the
distribution to have finite variance (required to rescale it to
\code{noise_sd}).}

\item{smooth_radius}{Integer \verb{>= 0}. Radius, in cells, of the focal mean
window used to introduce spatial autocorrelation into the noise (see
"How spatial autocorrelation is generated" above). \code{0} disables
this: cells get independent noise with no spatial structure beyond
the trend field itself. \code{1} (default) smooths over a 3x3 window;
larger values give smoother, more strongly autocorrelated fields, at
the cost of more computation.}

\item{spatial_rho}{Numeric in \verb{[0, 1]}. For the legacy model, the weight
assigned to focal-smoothed noise; this is not a target Moran's I. For
Gaussian and exponential covariance models, the adjacent-cell
correlation of the latent Gaussian field. It must be positive for these
formal models. It is ignored by the independent and Matérn models.}

\item{spatial_model}{Spatial noise model. \code{"legacy"} preserves the focal
smoother used by earlier versions. \code{"independent"}, \code{"gaussian"},
\code{"exponential"}, and \code{"matern"} use a formal stationary covariance
model simulated by circulant embedding and FFT.}

\item{spatial_range}{Positive covariance scale for the Matérn model.}

\item{spatial_smoothness}{Positive Matérn smoothness parameter.}

\item{signal_size}{One number or a two-number vector giving the height and
width, in cells, of block, square, rectangular, or elliptical signal
regions. Defaults to half the raster in each dimension for \code{"block"}
(its historical default), or a third of the raster for \code{"square"},
\code{"rectangle"} and \code{"ellipse"}.}

\item{signal_location}{\code{"centre"} or \code{"random"}; location of an exact
geometric signal region (\code{"block"}, \code{"square"}, \code{"rectangle"},
\code{"ellipse"} or \code{"custom"}). Has no effect on \code{"radial"} or \code{"gradient"},
which are not exact geometric regions.}

\item{signal_angle}{Rotation of a geometric signal region in degrees, or
\code{"random"} to draw a new orientation in each realisation.}

\item{signal_axis_ratio}{Positive width multiplier for an ellipse.}

\item{custom_mask}{Logical/numeric matrix, vector, or single-layer
\code{SpatRaster} defining the signal when \code{trend_shape = "custom"}.}

\item{constant_block}{Logical. If \code{TRUE}, sets a small block of cells to
a constant value (zero temporal variance) to exercise degenerate-case
handling. Independent of \code{trend_shape = "block"} above, which is
about the \emph{trend} pattern, not a zero-variance edge case.}

\item{break_type}{\code{"none"} (default): no structural break, this
function's original behaviour, unchanged. \code{"mean"}: a step change
in level at \code{break_time} -- ground truth for a change-point method
like Pettitt's test. \code{"slope"}: a change in slope at \code{break_time}
(continuous in level, a kink not a jump) -- ground truth for
distinguishing "did the trend change partway through" from "is
there a trend at all". A break and a monotonic trend
(\code{trend_strength}/\code{trend_fraction} above) compose independently on
any given cell -- a cell can have a trend only, a break only, both,
or neither; the two are not mutually exclusive, nor does one
replace the other.}

\item{break_time}{Integer or \code{NULL}. Time step \emph{after} which the
break takes effect (so \code{break_time = 5} means steps 1-5 are
unaffected, the break shows in step 6 onward). \code{NULL} (default):
the middle of the series, \code{round(n_time / 2)}. Must be in
\verb{[1, n_time - 1]} so there is at least one time step on each side.
Ignored when \code{break_type = "none"}.}

\item{break_fraction}{Fraction of the same spatially coherent blocks
used for \code{trend_fraction} above that get a break, independently of
which blocks (if any) got a trend. Default \code{0.3}. Ignored when
\code{break_type = "none"}.}

\item{break_magnitude}{Numeric. For \code{break_type = "mean"}: the size
of the step added to the level from \code{break_time} onward. For
\code{break_type = "slope"}: the size of the \emph{additional} slope (per
time step) that kicks in from \code{break_time} onward, on top of
whatever slope (possibly zero) that cell already had. Default \code{2}.
Ignored when \code{break_type = "none"}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}

\item{verbose}{Logical. If \code{TRUE}, report simulation progress, elapsed
duration and estimated time remaining. Set to \code{FALSE} inside large
Monte Carlo experiments when \code{\link[=benchmark_methods]{benchmark_methods()}} provides the outer
progress display.}
}
\value{
An object of class \code{"sptrends_simulation"} and \code{"sptrends"},
supporting \code{print()}, \code{summary()}, and \code{plot()}, with:
\item{series}{A \code{terra::SpatRaster} with \code{n_time} layers named
\code{"t1"}, \code{"t2"}, ...; each layer represents one time step. Pass this
to \code{\link[=workflow_tst]{workflow_tst()}} or the individual trend functions.}
\item{true_slope}{A single-layer \code{terra::SpatRaster}: the exact true
slope used to generate each cell's series (\code{0} where
\code{trend_fraction} assigned no trend). Compare directly against a
fitted \code{\link[=slope_estimator]{slope_estimator()}} to see estimation error, or against
\code{\link[=direction_map]{direction_map()}} to see which cells a method calls
significant vs. which cells truly have a trend.}
\item{true_signal}{A binary \code{SpatRaster} equal to \code{1} where the exact
true slope is non-zero.}
\item{true_direction}{A \code{SpatRaster} containing \code{-1}, \code{0}, or \code{1} for
the known trend direction.}
\item{true_break}{A single-layer \code{terra::SpatRaster}, \code{1} where a
structural break was actually applied (per \code{break_fraction}),
\code{0} elsewhere -- ground truth for evaluating a change-point
detection method the same way \code{true_slope} already lets you
evaluate a trend test, via \code{\link[=compare_detections]{compare_detections()}}. Always
present, even when \code{break_type = "none"} (all \code{0} in that
case), so downstream code doesn't need to branch on whether the
field exists.}
\item{break_time}{The actual time step used as the break point (see
\code{break_time} above), or \code{NULL} if \code{break_type = "none"} or
\code{break_fraction = 0} (no cell actually got a break).}
\item{parameters}{The data-generating parameters retained with the
realisation for reproducible benchmarking.}
\item{diagnostics}{Simulation metadata, including the covariance model
and target unit-lag correlation where applicable.}
}
\description{
Generates benchmark datasets with a known ground truth, for
evaluating spatiotemporal trend-detection methods. Concretely: a
synthetic gridded time series, with the true, known slope at every
cell kept alongside the data itself, for runnable \verb{@examples},
vignettes, and unit tests, rather than as a realistic environmental
simulation. Because the ground truth is known exactly (something no
real dataset offers), this is the tool behind sptrends' own worked
examples of what a trend-detection method actually gets right or
wrong: compare \code{true_slope} against a fitted method's estimated
slope, and against which cells it calls significant, to see
estimation error and Type I/Type II error directly instead of having
to take a method's output on faith -- the mechanism that lets power,
false discovery rate, sensitivity, specificity, and robustness be
demonstrated \emph{objectively} for any method, not just described in
prose.
}
\details{
\strong{Function type:} \strong{Benchmarking function} -- generates known-truth
data for \code{\link[=compare_detections]{compare_detections()}} and external methods. It is not a
component of the inferential workflows themselves.
}
\section{Typical use}{

Single-run benchmarking:

\if{html}{\out{<div class="sourceCode">}}\preformatted{sim_trend_stack()
    |
run one or more methods on sim$series
(trend_test(), workflow_tst(), workflow_rta(), or your own)
    |
compare_detections()
}\if{html}{\out{</div>}}

For replicated benchmarking, repeat simulation and detection across
seeds, collect the results, and aggregate them:

\if{html}{\out{<div class="sourceCode">}}\preformatted{list of detections + list of ground truths
    |
compare_detections(replicates = TRUE)
}\if{html}{\out{</div>}}

See \code{\link[=compare_detections]{compare_detections()}}'s examples for both routes worked through
in full. One call creates one replicate; Monte Carlo repetition
remains outside this function so the same simulator can benchmark
sptrends methods, future procedures, or external software. Use \code{ar1} for
temporal dependence and a formal \code{spatial_model} with its covariance
parameters for spatial dependence.
}

\section{Methodological details}{

\strong{Scope and limitations}

This function is intended for methodological benchmarking rather than
realistic environmental simulation. Its objective is to generate
controlled datasets with a known ground truth, not to reproduce the
statistical properties of a particular environmental variable such
as NDVI, temperature or precipitation. Formal Gaussian, exponential and
Matérn covariance models and exact geometric signal regions provide
controlled temporal and spatial structures for evaluating detection
methods; they are not a calibrated environmental process model.

\strong{How the trend is generated -- trend field}

\code{trend_shape} sets the \emph{pattern} of the true slope field:
\itemize{
\item \code{"radial"} (default): slope is \code{trend_strength} at the centre cell
and decays smoothly (exponentially) with distance from it, reaching
close to zero towards the edges.
\item \code{"gradient"}: slope varies linearly from \code{-trend_strength} at the
left edge to \code{+trend_strength} at the right edge -- decreasing on
one side, increasing on the other, with no radial symmetry.
\item \code{"block"}: \code{trend_strength} everywhere inside a centred square block
covering about half the raster's area, \code{0} everywhere outside it --
a sharp-edged region of trend against a flat background, rather than
a smooth spatial gradient.
}

\strong{How the trend is generated -- trend masking}

\code{trend_fraction} then decides \emph{how much of the raster actually has a
trend at all}: the raster is partitioned into coarse, contiguous
spatial blocks (not individual cells), a random \code{trend_fraction}
proportion of \emph{blocks} keep the slope values \code{trend_shape} assigned
them, and the rest are forced to an exact true slope of \code{0}, regardless
of shape. Acting on whole spatial patches rather than scattering
individual cells at random matters here: \code{\link[=trend_test]{trend_test()}}'s
whole rationale is borrowing statistical strength from a cell's
neighbours, which only helps when neighbouring cells are plausibly
trending together -- a cell-by-cell (salt-and-pepper) random mask would
quietly defeat that assumption and penalise CMK for no good reason.
\code{trend_fraction = 0} gives a complete null field (every cell's true
slope is exactly \code{0} -- there is no trend anywhere to find, the
sharpest way to check a method's false-positive rate). \code{trend_fraction = 1} gives a trend everywhere \code{trend_shape} says there should be one.
Among the blocks that do keep a trend, a random 10\% have their sign
flipped as a whole block, so the field is not purely "everything
increases" even within the trending region, while still keeping each
flipped patch spatially coherent.

\strong{How spatial autocorrelation is generated -- the idea}

For formal benchmarking, choose \code{spatial_model = "gaussian"},
\code{"exponential"}, or \code{"matern"}. Independent stationary fields are drawn
at each time step by circulant embedding and FFT. If an admissible
embedding is unavailable, modest grids use an exact covariance
eigendecomposition. For Gaussian and
exponential models, \code{spatial_rho} is the theoretical correlation between
horizontally or vertically adjacent cells. For Matérn fields,
\code{spatial_range} and \code{spatial_smoothness} define the covariance. The
\code{"independent"} model provides the exact zero-dependence baseline.

\code{spatial_model = "legacy"} retains the earlier didactic smoother.
Independent noise, with standard deviation \code{noise_sd}, is drawn per
cell and per time step (with AR(1) correlation across time, controlled
by \code{ar1}, but no spatial structure yet). If \code{smooth_radius > 0} and
\code{spatial_rho > 0}, each time step's noise layer is smoothed with a
focal mean filter. \code{smooth_radius} controls spatial scale, whereas
\code{spatial_rho} controls how strongly the original and smoothed fields
are blended. This is a
pragmatic moving-average smoother, not a fitted CAR/SAR model or a
Gaussian random field with a specified covariance function -- it is
meant to give \code{\link[=spatial_autocorrelation]{spatial_autocorrelation()}} something genuine to
detect in examples, not to match any particular real-world spatial
process (see "What this simulator is not" above).

\strong{Computational considerations}

\strong{How spatial autocorrelation is generated -- implementation}

The smoothing filter is a square window of side \code{2 * smooth_radius + 1} cells (via \code{terra::focal()}). The smoothed and blended noise are
rescaled so spatial controls do not intentionally change \code{noise_sd}.
\code{spatial_rho = 0} retains the independent field; \code{spatial_rho = 1}
retains the fully smoothed field and reproduces the behaviour used
before this parameter was added. Intermediate values are blend
weights, not target Moran's I values. All \code{n_time} layers are
smoothed in a single batched
\code{terra::focal()} call on the whole multi-layer noise stack, rather
than one call per layer -- \code{terra::focal()} already smooths each
layer of a multi-layer input independently (there is no cross-layer
mixing), so this produces identical output while avoiding
\code{n_time - 1} redundant round trips through \code{terra}'s internals.
Matters mainly for a large \code{n_time}; for the modest-sized rasters
this function is typically used to build (examples, tests), both
versions are effectively instant either way. \code{terra::focal()}
itself additionally requires the focal window (side
\code{2 * smooth_radius + 1}) to be no more than twice the raster's own
size in each direction -- a small raster with a proportionally
large \code{smooth_radius} (most sharply, any raster with \code{nrow} or
\code{ncol} of 1, since even the smallest window, \code{smooth_radius = 1},
already has side 3) will not fit this. Rather than letting
\code{terra::focal()}'s own internal error propagate up, this is checked
beforehand and, if violated, a warning is issued and unsmoothed
noise is used for that call instead.

\strong{Statistical assumptions and interpretation}

\strong{Noise distribution and comparing methods}

\code{noise_dist} controls the \emph{shape} of the noise, independently of its
standard deviation (\code{noise_sd}) or its temporal (\code{ar1}) and spatial
(\code{smooth_radius}) correlation. This matters specifically for comparing
a rank-based method (Mann-Kendall, Contextual Mann-Kendall, Theil-Sen)
against a parametric one (e.g. ordinary least squares): under Gaussian
noise (\code{noise_dist = "gaussian"}, the default), OLS is the more
\emph{efficient} estimator, so a comparison run only under Gaussian noise
will not show the robustness advantage rank-based methods are
typically chosen for. Set \code{noise_dist = "t"} with a low \code{t_df} (e.g.
\code{3} or \code{4}) to generate heavy-tailed, outlier-prone noise instead, at
the same \code{noise_sd} -- this is the condition under which rank-based
methods are expected to hold up better than OLS.

The simulation parameters control a synthetic data-generating process;
they are not fitted environmental parameters. Under \code{spatial_model = "legacy"}, \code{spatial_rho} is a blend weight rather than a target
correlation. Under Gaussian or exponential covariance with Gaussian
noise, it is the target correlation between horizontally or vertically
adjacent cells. With Student-t noise, it controls the latent Gaussian
dependence used by the copula, so realised Pearson correlation need not
equal it exactly. One call produces one realisation; use
\code{\link[=benchmark_methods]{benchmark_methods()}} for a Monte Carlo experiment.

\strong{Quality assurance}

Tests verify known slope and break fields, reproducibility, null and
complete-signal cases, noise scale and distribution, degenerate grid
sizes, focal-window safeguards, temporal/spatial controls and direct
compatibility with \code{\link[=compare_detections]{compare_detections()}}. The \code{spatial_rho} tests
specifically protect both endpoint semantics and the pre-0.89 default.
Independent validation additionally compared the analytical Matérn
correlation with \code{fields::Matern()}, evaluated 1,000 Gaussian fields per
spatial model, and checked temporal AR(1), marginal distributions, exact
truth fields, detection metrics and reproducibility. All 33 external
validation controls passed in the recorded 0.96.3 full run. The retained
script and numerical summaries are described under \code{inst/validation}.
See \code{?sptrends} for the package-wide release-check protocol.
}

\examples{
# A small synthetic dataset with a known trend -- 8 time steps on a
# 12x12 grid. terra::global(..., "range") shows the true slope's
# minimum and maximum across all cells.
sim <- sim_trend_stack(nrow = 12, ncol = 12, n_time = 8, seed = 42)
terra::nlyr(sim$series)
terra::global(sim$true_slope, "range", na.rm = TRUE)

# A complete null field: every true slope is exactly zero.
sim_null <- sim_trend_stack(nrow = 12, ncol = 12, n_time = 8,
                             trend_fraction = 0, seed = 1)
terra::global(sim_null$true_slope, "range", na.rm = TRUE)

# Ground truth for a change-point method (e.g. Pettitt's test): a
# mean-shift break in 30\% of the map, no monotonic trend at all.
sim_break <- sim_trend_stack(nrow = 12, ncol = 12, n_time = 10,
                              trend_fraction = 0, break_type = "mean",
                              break_fraction = 0.3, seed = 2)
sim_break$break_time
terra::global(sim_break$true_break, "sum", na.rm = TRUE)

# Spatial scale and intensity are separate: smooth_radius defines the
# focal scale, while spatial_rho blends independent and smoothed noise.
# Compare Moran's I for one time step at the two intensity extremes.
\donttest{
r0 <- sim_trend_stack(nrow = 20, ncol = 20, n_time = 1,
                       smooth_radius = 3, spatial_rho = 0,
                       seed = 1)$series[[1]]
r1 <- sim_trend_stack(nrow = 20, ncol = 20, n_time = 1,
                       smooth_radius = 3, spatial_rho = 1,
                       seed = 1)$series[[1]]
spatial_autocorrelation(r0, nperm = 99, seed = 1, verbose = FALSE,
                         report = FALSE)$statistic
spatial_autocorrelation(r1, nperm = 99, seed = 1, verbose = FALSE,
                         report = FALSE)$statistic
}

# Heavy-tailed (outlier-prone) noise instead of Gaussian, at the same
# standard deviation -- for comparing a rank-based method against OLS.
sim_heavy <- sim_trend_stack(nrow = 10, ncol = 10, n_time = 10,
                              noise_dist = "t", t_df = 3, seed = 1)
terra::nlyr(sim_heavy$series)

}
\references{
Dietrich, C.R. and Newsam, G.N. (1997). Fast and exact simulation of
stationary Gaussian processes through circulant embedding of the
covariance matrix. \emph{SIAM Journal on Scientific Computing}, 18(4),
1088-1107. \doi{10.1137/S1064827592240555}
}
\seealso{
Other example data functions:
\code{\link[=example_data]{example_data()}}
}
\concept{example data functions}
