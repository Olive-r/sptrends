% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sptrends-methods.R
\name{plot.sptrends}
\alias{plot.sptrends}
\title{Plot a sptrends result}
\usage{
\method{plot}{sptrends}(x, ...)
}
\arguments{
\item{x}{An object of class \code{"sptrends"} (also one of \code{"tst"},
\code{"rta"}, \code{"workflow_trends"}, \code{"trend_test"}, \code{"slope"},
\code{"prewhiten"}, \code{"fdr"},
\code{"spatial_autocorrelation"}, \code{"compare_detections"},
\code{"sptrends_simulation"}, \code{"sptrends_simulation_design"}, or
\code{"sptrends_benchmark"}),
from \code{\link[=workflow_tst]{workflow_tst()}}, \code{\link[=workflow_rta]{workflow_rta()}}, \code{\link[=workflow_trends]{workflow_trends()}},
\code{\link[=trend_test]{trend_test()}},
\code{\link[=slope_estimator]{slope_estimator()}}, \code{\link[=prewhiten]{prewhiten()}}, \code{\link[=fdr_correction]{fdr_correction()}},
\code{\link[=spatial_autocorrelation]{spatial_autocorrelation()}}, \code{\link[=compare_detections]{compare_detections()}},
\code{\link[=sim_trend_stack]{sim_trend_stack()}}, \code{\link[=simulation_design]{simulation_design()}}, or \code{\link[=benchmark_methods]{benchmark_methods()}}.}

\item{...}{Passed on to the underlying plotting logic -- see each
section below for the arguments (typically \code{which}, and sometimes
\code{method}, \code{smooth}, \code{alpha}, or \code{path}) \code{x}'s own class accepts.}
}
\value{
\code{x}, invisibly.
}
\description{
The graphical exploration counterpart to \code{print()}'s one-line
overview and \code{summary()}'s textual report -- see \code{\link[=print.sptrends]{print.sptrends()}}
for the full class list and the rationale for one shared entry
point per generic. What \code{which} (and any other named
argument) accepts depends entirely on \code{x}'s own class -- see the
sections below, grouped by what kind of result each class
represents.
}
\details{
\strong{Function type:} \strong{Reporting/derived function} -- visualises an existing
result and does not alter or recompute its statistical analysis.
}
\section{Typical use}{

\verb{result <- workflow_tst(x); plot(result)} draws the class-specific default;
use \code{which} for an alternative diagnostic view where supported.
}

\section{Methodological details}{

\strong{Published workflow: \code{"tst"}.}
By default, draws a binarised trend map: increases and decreases from
the selected trend statistic are retained only where the selected
multiple-testing procedure rejects the null hypothesis (see
\code{\link[=direction_map]{direction_map()}}).

\code{which} -- the default and the three main views:\tabular{ll}{
   \code{which} \tab Draws \cr
   \code{"direction"} (default) \tab Binarised trend map after FDR correction (titled "TST direction map"; "RTA direction map" for the \code{"rta"} case below) \cr
   \code{"significance"} \tab FDR-BH/FDR-BKY significance maps side by side \cr
   \code{"trend"} \tab Uncorrected trend statistic/p-value/significance/direction \cr
   \code{"slope"} \tab Theil-Sen slope, masked to significant cells \cr
}


\code{which = "trend"} draws \strong{uncorrected} diagnostics only -- see the
"Warning" section of \code{?trend_test} before reporting significance
from this view. \code{which = "slope"} is the more reliable source for
per-cell direction/magnitude than the CMK statistic \code{Sm}, since
\code{Sm}'s neighbourhood averaging can occasionally disagree in sign
with a cell's own Theil-Sen estimate in a spatially heterogeneous
neighbourhood (see "Display smoothing" below for how this view
handles that visually).

Eight further views, all \strong{uncorrected} (no FDR, no significance
masking) -- diagnostics on the raw slope or the raw p-value, not a
final result to report:\tabular{ll}{
   \code{which} \tab Draws \cr
   \code{"slope_map"} \tab Continuous Theil-Sen slope, unmasked \cr
   \code{"slope_direction"} \tab Binary sign of the slope, unmasked \cr
   \code{"slope_hist"} \tab Histogram of slope values with a density curve \cr
   \code{"slope_bar"} \tab Bar chart of positive/negative/zero slope counts \cr
   \code{"pvalue_map"} \tab Continuous, uncorrected p-value \cr
   \code{"pvalue_significance"} \tab Binary significant/not at \code{alpha} \cr
   \code{"pvalue_hist"} \tab Classic binned histogram of p-values \cr
   \code{"pvalue_bar"} \tab Bar chart of significant vs. not \cr
}


\code{slope_map}/\code{slope_direction}/\code{slope_hist}/\code{slope_bar} need
\code{x$theil_sen} (i.e. \code{workflow_tst()} must have been run with
\code{theil_sen = TRUE}); the four \verb{pvalue_*} views always work, since
\code{x$trend} is never \code{NULL}. \code{slope_bar}/\code{pvalue_bar} accept
\code{probability = TRUE} for percentages instead of counts;
\code{pvalue_significance} uses \code{alpha} (default \code{0.05}), uncorrected
for multiple testing.

\code{method}: \code{"BKY"} (default, matching \code{workflow_tst()}'s default
\code{fdr_method}) or \code{"BH"} -- which correction to use for
\code{which = "direction"} or \code{which = "slope"}. Ignored otherwise. If
\code{x} only has the other one (e.g. \code{workflow_tst()} was called with
\code{fdr_method = "BH"}), set \code{method} to match.
\strong{Published workflow: \code{"rta"}.}
By default, draws the map you actually want to report: direction of
change masked by FDR-BH significance (see \code{\link[=direction_map]{direction_map()}}).
Unlike the \code{"tst"} case above, there is no \code{method} argument --
\code{workflow_rta()} always uses FDR-BH (see \code{?workflow_rta}'s "How RTA
differs from TST,
and why"), so there is nothing to choose between.

\code{which}: \code{"direction"} (default), \code{"significance"}, \code{"trend"},
\code{"slope"}, and the eight further \verb{slope_*}/\verb{pvalue_*} diagnostic
views -- same meaning as the \code{"tst"} case above, but always
FDR-BH.
\strong{Configurable workflow: \code{"workflow_trends"}.}
Provides the same trend, slope, p-value, significance and direction
views, using whichever optional slope and FDR stages were selected.
\strong{Display smoothing (\code{"tst"}/\code{"rta"}/\code{"workflow_trends"},
\code{which = "slope"} only).}
\code{smooth}: logical. If \code{TRUE}
(default) and \code{x$theil_sen} (\code{x$slope} for a \code{"workflow_trends"}
object -- same mechanism, different field name) was \strong{not} already
smoothed at source
(via \code{workflow_tst()}'s own \code{theil_sen_args = list(smooth_neighbourhood = TRUE)}, which is \emph{not} \code{workflow_tst()}'s own default -- see
\code{?workflow_tst}'s
"Computational considerations" section for why \code{workflow_tst()}
and \code{\link[=workflow_rta]{workflow_rta()}} deliberately do not differ here), the
significant-cells-only slope map is smoothed with a queen-3x3 median
filter \strong{for this plot only}; if it was already smoothed at source,
this is not applied a second time. \code{x$theil_sen} itself is never
modified by plotting, and neither is any part of the significance
decision; this only changes what gets drawn. Because smoothing here
runs \emph{after} masking to significant cells (a necessarily sparser set
of cells than the full raster), the result can look visually
blockier than smoothing a dense, unmasked raster would -- this is an
expected consequence of masking before smoothing, not a bug (see
\code{.mask_and_smooth_slope()}'s own \code{na.policy = "omit"} for the
related, and separate, fix ensuring a non-significant cell is never
itself painted with a colour). Setting \code{smooth = FALSE} when \code{workflow_tst()}
already smoothed at source cannot recover the unsmoothed values
(they were never kept) -- a message explains this if it happens. The
plot title always states which stage (if any) applied smoothing, so
the display is never ambiguous about what it shows. See
\code{?slope_estimator}'s "Optional queen-neighbourhood smoothing"
section for why this is a display convenience, not a validated
estimator, and is not applied to the value returned by \code{\link[=workflow_tst]{workflow_tst()}}.
\strong{Trend estimation: \code{"trend_test"}.}
\code{which}: \code{"maps"} (default), all four uncorrected diagnostic maps
(trend statistic, p-value, significance, and direction), via
\code{trend_maps()}; \code{"histograms"}, histograms of the trend statistic and
p-value, via \code{trend_histograms()}. These are the \strong{uncorrected}
result; see the "Warning" section of \code{?trend_test}
before reporting significance from them without a multiple-testing
correction (see \code{\link[=fdr_correction]{fdr_correction()}} and the \code{"fdr"} case below).

\code{alpha}: significance threshold, only used when \code{which = "maps"}.
\strong{Trend estimation: \code{"slope"}.}
Draws the zero-centred diverging slope map; no \code{which} argument (only
one view exists for this class).
\strong{Diagnostic: \code{"prewhiten"}.}
\code{which}: \code{"maps"} (default), the four diagnostic maps; \code{"histograms"},
the two diagnostic histograms.
\strong{Diagnostic: \code{"fdr"}.}
\code{which}: \code{"significance"} (default), significance maps (raster input
only); \code{"pvalue_histogram"}, histogram of the raw input p-values;
\code{"comparison"}, bar chart comparing significant counts across
whichever of raw/BH/BKY/BY were actually requested;
\code{"threshold"}, the BH vs. BKY step-up threshold plot (only if BKY was
requested).
\strong{Diagnostic: \code{"spatial_autocorrelation"}.}
Global results draw the null distribution with the observed statistic
marked. Local results draw the statistic, empirical z, raw permutation
p-value and exploratory raw-significance map. Apply and plot
\code{\link[=fdr_correction]{fdr_correction()}} separately for BH, BKY or BY inference.
\strong{Validation: \code{"compare_detections"}.}
Intended mainly for simulation studies -- not typically the plot an
analyst runs on a real dataset, since it needs a known ground truth
to have been scored against in the first place (see
\code{\link[=compare_detections]{compare_detections()}}).

A grouped bar chart of the comparison table -- one group of bars per
method, one bar per metric; no \code{which} argument (only one view
exists for this class). \code{metrics} chooses which columns to plot
(defaults to all of them).
\strong{Simulation and benchmarking.}
Simulation plots expose the known signal, slope, direction, breaks, or
complete raster series. Design plots show the number of levels per factor.
Benchmark plots show performance against a varying scenario factor using
lines with uncertainty, grouped bars, replicate boxplots, two-factor
heatmaps, or multi-metric profiles. Use \code{metric}, \code{scenario}, \code{group},
\code{facet}, \code{type}, \code{interval}, and \code{level} to configure these views.
Confidence intervals use the between-replicate standard error and a
Student-t critical value; intervals for rates and powers are clipped to
their admissible range from zero to one.
}

\examples{
\donttest{
# Annual mean NDVI from the bundled environmental dataset.
r <- read_ordered_stack(example_data("vhp_ndvi"))
result <- workflow_tst(r, report = FALSE, verbose = FALSE)

# Default map: direction of change (greening/browning/no change),
# masked by FDR-BKY significance -- cells with grey are not
# significant, so they are left out of the coloured pattern.
plot(result)

# The rate of change (not just direction) among significant cells,
# queen-3x3 median smoothed for display (the default for this view).
plot(result, which = "slope")
}

}
\seealso{
\code{\link[=print.sptrends]{print.sptrends()}} for a concise overview and
\code{\link[=summary.sptrends]{summary.sptrends()}} for detailed textual output.
}
