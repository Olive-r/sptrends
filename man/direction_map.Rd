% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direction.R
\name{direction_map}
\alias{direction_map}
\title{Binarised trend map after multiple-testing correction}
\usage{
direction_map(
  trend,
  fdr_result,
  slope = NULL,
  method = c("BH", "BKY", "BY", "raw"),
  verbose = TRUE
)
}
\arguments{
\item{trend}{The \verb{$stats} field of \code{\link[=trend_test]{trend_test()}}'s output.}

\item{fdr_result}{Output of \code{\link[=fdr_correction]{fdr_correction()}}, run on \code{trend$stats$p}.}

\item{slope}{Optional single-layer \code{SpatRaster} (e.g.
\code{\link[=slope_estimator]{slope_estimator()}}'s own \verb{$slope}) whose sign determines direction
instead of the trend test's own statistic. \code{NULL} (default): use
\code{trend}'s own \code{Sm}/\code{S}/\code{beta}, as before. Direction from the slope and
direction from the trend statistic usually agree but are not
guaranteed to: a cell with a near-flat slope of its own can still
inherit its neighbours' sign in \code{Sm} under CMK's neighbourhood
averaging. The significance mask (which cells are shown as
increasing/decreasing at all, from \code{fdr_result}) is identical either
way -- only the source of the \emph{sign} for already-significant cells
changes.}

\item{method}{\code{"BH"} (default), \code{"BKY"}, \code{"BY"}, or \code{"raw"} (uncorrected,
included for comparison) -- which rejection vector in \code{fdr_result} to
use as the significance mask.}

\item{verbose}{Logical. Print a one-line count summary.}
}
\value{
A single-layer \code{terra::SpatRaster}, values \code{-1} (significant
decrease), \code{0} (not significant), \code{1} (significant increase), named
\code{"binarised_trend_map"}.
}
\description{
Combines the sign of the trend statistic (\code{Sm}/\code{S}) with a chosen
FDR-corrected (or raw) rejection vector to classify each cell as an
increase, a decrease, or a non-significant result -- a binarised map
for reporting \emph{after} multiple-comparison correction, as
opposed to \code{\link[=trend_maps]{trend_maps()}}, which uses the uncorrected p-value.
}
\details{
\strong{Function type:} \strong{Support function} -- computes something real
(a genuinely new
raster, combining trend direction with significance), but is not one
of the core building blocks of TST or RTA itself; it is a
post-processing step that consumes the output of two of them
(\code{\link[=trend_test]{trend_test()}} and \code{\link[=fdr_correction]{fdr_correction()}}) together. Not exported --
the binarised trend direction is the same underlying
computation as the uncorrected direction already reachable via
\code{plot(x, which = "trend")}, just with a significance filter applied
on top; reachable directly via \code{plot(x, which = "direction")} for
\code{workflow_tst()}/\code{workflow_rta()} results, or with \code{:::} for
programmatic use.
}
\examples{
\donttest{
r <- read_ordered_stack(example_data("vhp_ndvi"))
trend <- trend_test(r, report = FALSE, verbose = FALSE)
fdr_result <- fdr_correction(trend$stats$p, report = FALSE, verbose = FALSE)

# Combines "is it significant" (from fdr_result) with "which way is
# it going" (from trend) into a single binarised trend map.
direction <- sptrends:::direction_map(trend$stats, fdr_result, method = "BH")
}

}
\references{
This combination of FDR-corrected significance with trend direction is
this package's own contribution, not from an external method paper;
cited here as the source of the overall TST workflow it belongs to:
\itemize{
\item Gutiérrez-Hernández, O. and García, L.V. (2025) Uncovering true
significant trends in global greening. Remote Sensing Applications:
Society and Environment, 37, 101377. \doi{10.1016/j.rsase.2024.101377}
}

Underlying theoretical justification for the FDR-BH assumption this
significance is based on:
\itemize{
\item Benjamini, Y., & Yekutieli, D. (2001) The control of the false
discovery rate in multiple testing under dependency. Annals of
Statistics, 29(4), 1165-1188. \doi{10.1214/aos/1013699998}
}
}
\keyword{internal}
