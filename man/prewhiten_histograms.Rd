% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prewhiten.R
\name{prewhiten_histograms}
\alias{prewhiten_histograms}
\title{Histograms of prewhitening diagnostics}
\usage{
prewhiten_histograms(diagnostics, path = NULL)
}
\arguments{
\item{diagnostics}{The \code{diagnostics} element returned by
\code{\link[=prewhiten]{prewhiten()}}.}

\item{path}{Character or \code{NULL}. If supplied, PNGs are written using
this as a filename prefix: setting \code{path} to \code{out} writes files named
\code{out_dw.png} and \code{out_rho.png}.}
}
\value{
\code{NULL}, invisibly. Called for its plotting side effect.
}
\description{
\strong{Function type:} \strong{Reporting/derived function} -- summarises or
plots the output of
another function; it does not compute any new statistic. Not
exported -- called internally by \code{report = TRUE}, and reachable
from outside the package via \code{plot(x, which = "histograms")}.
}
\examples{
r <- read_ordered_stack(example_data("vhp_ndvi"))
pw <- prewhiten(r, report = FALSE, verbose = FALSE)

# Distributions of the Durbin-Watson statistic and the estimated rho
# across all cells, before vs. after prewhitening. Called internally
# by plot() on a prewhiten() result -- the public entry point is:
plot(pw, which = "histograms")

}
\references{
\itemize{
\item Wang, X.L. and Swail, V.R. (2001) Changes of Extreme Wave Heights in
Northern Hemisphere Oceans and Related Atmospheric Circulation
Regimes. Journal of Climate, 14(10), 2204-2221.
}
}
\seealso{
Other Prewhitening functions:
\code{\link[=prewhiten]{prewhiten()}},
\code{\link[=prewhiten_maps]{prewhiten_maps()}},
\code{\link[=prewhiten_summary]{prewhiten_summary()}}
}
\concept{Prewhitening functions}
\keyword{internal}
