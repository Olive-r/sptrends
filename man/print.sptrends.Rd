% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sptrends-methods.R
\name{print.sptrends}
\alias{print.sptrends}
\title{Print a sptrends result}
\usage{
\method{print}{sptrends}(x, ...)
}
\arguments{
\item{x}{An object of class \code{"sptrends"} (also one of \code{"tst"},
\code{"rta"}, \code{"workflow_trends"}, \code{"trend_test"}, \code{"slope"},
\code{"prewhiten"}, \code{"fdr"},
\code{"spatial_autocorrelation"}, \code{"compare_detections"},
\code{"sptrends_simulation"}, \code{"sptrends_simulation_design"}, or
\code{"sptrends_benchmark"}),
from \code{\link[=workflow_tst]{workflow_tst()}}, \code{\link[=workflow_rta]{workflow_rta()}}, \code{\link[=workflow_trends]{workflow_trends()}},
\code{\link[=trend_test]{trend_test()}},
\code{\link[=slope_estimator]{slope_estimator()}}, \code{\link[=prewhiten]{prewhiten()}}, \code{\link[=fdr_correction]{fdr_correction()}},
\code{\link[=spatial_autocorrelation]{spatial_autocorrelation()}}, \code{\link[=compare_detections]{compare_detections()}},
\code{\link[=sim_trend_stack]{sim_trend_stack()}}, \code{\link[=simulation_design]{simulation_design()}}, or \code{\link[=benchmark_methods]{benchmark_methods()}}.}

\item{...}{Ignored.}
}
\value{
\code{x}, invisibly.
}
\description{
A quick, one-line-per-detail overview of any classed object this
package returns -- \code{workflow_tst()}, \code{workflow_rta()},
\code{\link[=workflow_trends]{workflow_trends()}},
\code{\link[=trend_test]{trend_test()}}, \code{\link[=slope_estimator]{slope_estimator()}},
\code{\link[=prewhiten]{prewhiten()}}, and \code{\link[=fdr_correction]{fdr_correction()}} all return an object
with \code{"sptrends"} as (one of) its classes, and \code{print()},
\code{\link[=summary.sptrends]{summary.sptrends()}}, and \code{\link[=plot.sptrends]{plot.sptrends()}} all work the same way
regardless of which one you have -- the specific one-line summary
shown depends on \code{x}'s own class, listed below. This mirrors the
convention used throughout \code{terra} itself (a single \code{print()},
whether \code{x} is a \code{SpatRaster} or a \code{SpatVector}): one predictable
entry point per generic, not a different function name to remember
for each result type. The API is organised around the \emph{object} a
function returns, not around remembering which reporting function
goes with which: \code{x <- workflow_tst(...)}, then \code{print(x)},
\code{summary(x)}, \code{plot(x)}, regardless of what \code{x} actually is.
}
\details{
\tabular{ll}{
   Generic \tab Purpose \cr
   \code{print()} \tab Quick overview \cr
   \code{summary()} \tab Detailed textual report \cr
   \code{plot()} \tab Visual exploration \cr
}


\code{print()} itself is intended for a quick inspection of an object at
the console; use \code{\link[=summary.sptrends]{summary.sptrends()}} for more detailed textual
reporting and \code{\link[=plot.sptrends]{plot.sptrends()}} for graphical exploration.

\strong{Function type:} \strong{Reporting/derived function} -- presents an existing
result and does not compute a new statistical estimate.
}
\section{Typical use}{

\verb{result <- workflow_tst(x); print(result)} for a concise console overview.
}

\section{Methodological details}{

\strong{Published workflow: \code{"tst"}.}
Whether prewhitening ran, how many cells were tested, and how many
are significant after FDR correction (if run).
\strong{Published workflow: \code{"rta"}.}
The Theil-Sen slope range, the trend test's cell count, and how many
cells are significant after FDR-BH correction.
\strong{Configurable workflow: \code{"workflow_trends"}.}
The selected preprocessing, trend-test, slope and FDR stages,
including skipped optional stages and the qualified Moran assessment
when requested.
\strong{Trend estimation: \code{"trend_test"}.}
How many cells were tested, and how many are significant at the
conventional alpha=0.05 threshold, uncorrected.
\strong{Trend estimation: \code{"slope"}.}
How many cells have a valid slope, and its range.
\strong{Diagnostic: \code{"prewhiten"}.}
How many cells were prewhitened, out of how many valid cells.
\strong{Diagnostic: \code{"fdr"}.}
How many cells are significant under each method that was requested
(raw, BH, BKY, and BY, if it was explicitly requested -- see
\code{?fdr_correction}'s own \code{method} argument for why \code{"BY"} is opt-in,
not part of its own default).
\strong{Diagnostic: \code{"spatial_autocorrelation"}.}
Global results show the observed statistic (Moran's I or Getis-Ord
General G), its sign where applicable, and its permutation p-value.
Local results show the valid-cell count and the exploratory number of
cells below the raw \code{alpha}, followed by the route to
\code{\link[=fdr_correction]{fdr_correction()}} for BH, BKY or BY.
\strong{Validation: \code{"compare_detections"}.}
The comparison table itself, printed as a plain data frame (this
class also inherits from \code{"data.frame"}, so indexing, \code{$}, and so on
all work exactly as they would on any other one).
\strong{Simulation and benchmarking.}
Simulation objects report their dimensions, dependence model and known
signal. Designs report scenario counts and varied factors. Benchmarks
report their stage, methods, scenarios, replicates and elapsed time.
}

\examples{
\donttest{
# Annual mean NDVI from the bundled environmental dataset.
r <- read_ordered_stack(example_data("vhp_ndvi"))
result <- workflow_tst(r, report = FALSE, verbose = FALSE)
print(result)  # dispatches to the "tst" case above
}

}
\seealso{
\code{\link[=summary.sptrends]{summary.sptrends()}} for detailed textual output and
\code{\link[=plot.sptrends]{plot.sptrends()}} for graphical exploration.
}
