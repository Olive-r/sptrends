% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fdr.R
\name{fdr_direction_summary}
\alias{fdr_direction_summary}
\title{Compare direction of change across raw, FDR-BH, and FDR-BKY}
\usage{
fdr_direction_summary(
  trend,
  fdr_result,
  slope = NULL,
  methods = c("raw", "BH", "BKY", "BY"),
  path = NULL
)
}
\arguments{
\item{trend}{The \verb{$stats} field of \code{\link[=trend_test]{trend_test()}}'s output.}

\item{fdr_result}{Output of \code{\link[=fdr_correction]{fdr_correction()}}, run on \code{trend$stats$p}.}

\item{slope}{Optional single-layer \code{SpatRaster} (e.g.
\code{\link[=slope_estimator]{slope_estimator()}}'s own \verb{$slope}) whose sign determines direction
instead of the trend test's own statistic. \code{NULL} (default): use
\code{trend}'s own \code{Sm}/\code{S}/\code{beta}, matching \code{\link[=direction_map]{direction_map()}}'s own
default.}

\item{methods}{Character vector of methods to include, matching
whichever rejection vectors are present in \code{fdr_result}
(\code{c("raw", "BH", "BKY", "BY")} by default -- methods not present
in \code{fdr_result} are skipped with a message, not an error).}

\item{path}{Character or \code{NULL}. If supplied, write the table to this
CSV path.}
}
\value{
Invisibly, a data frame with one row per method:
\code{n_increase}, \code{n_decrease}, \code{n_not_significant}, \code{pct_increase},
\code{pct_decrease}.
}
\description{
A single table with one row per correction method, so you can see how
the significant-increase/decrease counts shrink (or don't) as the
correction gets stricter.
}
\details{
\strong{Function type:} \strong{Reporting/derived function} -- summarises or
plots the output of
another function; it does not compute any new statistic.
}
\examples{
r <- read_ordered_stack(example_data("vhp_ndvi"))
trend <- trend_test(r, report = FALSE, verbose = FALSE)
fdr_result <- fdr_correction(trend$stats$p, report = FALSE, verbose = FALSE)

# A table version of direction_map(): cell counts and percentages
# for increase/decrease/not-significant results, raw and the selected FDR
# methods side by side (BH and BKY by default; BY when explicitly
# requested).
sptrends:::fdr_direction_summary(trend$stats, fdr_result)

}
\references{
This combination of FDR-corrected significance with trend direction is
this package's own contribution, not from an external method paper;
cited here as the source of the overall TST workflow it belongs to:
\itemize{
\item Gutiérrez-Hernández, O. and García, L.V. (2025) Uncovering true
significant trends in global greening. Remote Sensing Applications:
Society and Environment, 37, 101377. \doi{10.1016/j.rsase.2024.101377}
}

Underlying theoretical justification for the FDR-BH assumption this
significance is based on:
\itemize{
\item Benjamini, Y., & Yekutieli, D. (2001) The control of the false
discovery rate in multiple testing under dependency. Annals of
Statistics, 29(4), 1165-1188. \doi{10.1214/aos/1013699998}
Not exported. Same reasoning as \code{\link[=fdr_direction_plot]{fdr_direction_plot()}}: it takes
\code{trend}/\code{fdr_result} as two separate objects rather than one classed
object, so there is nothing for a single S3 method to dispatch on.
Unlike every other function internalised alongside it, no S3 method
calls this one either -- it has no reporting/derived function
equivalent left reachable at all from outside the package (other than
\code{:::}). \code{direction_map()} (also not exported) plus your own
tabulation is the closest standalone alternative.
}
}
\seealso{
Other FDR correction functions:
\code{\link[=fdr_bh]{fdr_bh()}},
\code{\link[=fdr_bky]{fdr_bky()}},
\code{\link[=fdr_by]{fdr_by()}},
\code{\link[=fdr_comparison_barplot]{fdr_comparison_barplot()}},
\code{\link[=fdr_correction]{fdr_correction()}},
\code{\link[=fdr_direction_plot]{fdr_direction_plot()}},
\code{\link[=fdr_pvalue_histogram]{fdr_pvalue_histogram()}},
\code{\link[=fdr_significance_maps]{fdr_significance_maps()}},
\code{\link[=fdr_summary]{fdr_summary()}},
\code{\link[=fdr_threshold_plot]{fdr_threshold_plot()}}
}
\concept{FDR correction functions}
\keyword{internal}
