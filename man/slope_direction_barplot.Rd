% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slope_estimator.R
\name{slope_direction_barplot}
\alias{slope_direction_barplot}
\title{Bar chart of positive/negative/zero counts for a Theil-Sen slope raster}
\usage{
slope_direction_barplot(slope, probability = FALSE, ...)
}
\arguments{
\item{slope}{A \code{SpatRaster} of Theil-Sen slopes, e.g.
\code{slope_estimator()}'s own \verb{$slope}.}

\item{probability}{Logical. If \code{TRUE}, bars show percentage of valid
cells rather than raw counts. Default \code{FALSE}.}

\item{...}{Passed on to \code{\link[graphics:barplot]{graphics::barplot()}}.}
}
\value{
Invisibly, a named numeric vector with the raw counts
(\code{Positive}, \code{Negative}, \code{Zero}), regardless of \code{probability}.
}
\description{
A bar chart of how many valid cells have a positive, negative, or
exactly zero Theil-Sen slope -- the same classification
\code{\link[=slope_direction_map]{slope_direction_map()}} draws spatially, summarised here as raw
counts (or percentages).
}
\details{
\strong{Function type:} \strong{Reporting/derived function} -- summarises or
plots the output of
another function; it does not compute any new statistic. Not
exported -- called internally by \code{report = TRUE}, and reachable
from outside the package via \code{plot()}.
}
\examples{
r <- read_ordered_stack(example_data("vhp_ndvi"))
slope <- slope_estimator(r, report = FALSE, verbose = FALSE)
plot(slope, which = "bar")

}
\seealso{
Other Theil-Sen slope functions:
\code{\link[=slope_direction_map]{slope_direction_map()}},
\code{\link[=slope_histogram]{slope_histogram()}}
}
\concept{Theil-Sen slope functions}
\keyword{internal}
