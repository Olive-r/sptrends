% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation-design.R
\name{simulation_design}
\alias{simulation_design}
\title{Build a factorial design of simulation scenarios}
\usage{
simulation_design(..., constants = list(), prefix = "scenario", verbose = TRUE)
}
\arguments{
\item{...}{Named vectors or lists of factor levels to cross.}

\item{constants}{Named list of arguments shared by every scenario.}

\item{prefix}{Character prefix used for generated scenario names.}

\item{verbose}{Logical. If \code{TRUE}, reports progress, elapsed time and the
estimated time remaining while scenarios are assembled.}
}
\value{
A named list of argument lists suitable for the \code{scenarios}
argument of \code{\link[=benchmark_methods]{benchmark_methods()}}, with classes
\code{"sptrends_simulation_design"} and \code{"sptrends"} for unified
printing, summaries, and plotting.
}
\description{
Creates named argument lists for \code{\link[=sim_trend_stack]{sim_trend_stack()}} by crossing temporal,
spatial, signal and noise conditions. It separates experimental design from
data generation so the complete scenario grid can be inspected and retained.
}
\details{
\strong{Function type:} \strong{Benchmarking function} -- defines simulation
scenarios; it does not generate data or perform inference.
}
\section{Typical use}{

Define the factors that should vary, add shared settings through \code{constants},
and pass the returned list to \code{\link[=benchmark_methods]{benchmark_methods()}}.
}

\section{Methodological details}{

\strong{Experimental design}

Every combination is retained. This makes comparisons across spatial and
temporal dependence explicit and prevents methods from being evaluated on
accidentally different scenario sets. Values that must remain grouped,
such as a two-number \code{signal_size}, should be wrapped in a list.

\strong{Computational considerations}

This function only constructs argument lists. Memory use grows with the
product of the numbers of supplied factor levels; data generation remains
deferred to \code{\link[=benchmark_methods]{benchmark_methods()}}.

\strong{Limitations}

A full factorial design can become unnecessarily large. Users should vary
scientifically relevant factors and keep fixed settings in \code{constants}.

\strong{Quality assurance}

Tests verify factorial completeness, deterministic ordering, grouped
values, validation failures, metadata retention and S3 presentation. The
complete external simulation-cycle validation passed all 33 prespecified
controls; see \verb{inst/validation/} for the retained protocol and results.
}

\examples{
design <- simulation_design(
  spatial_model = c("independent", "exponential"),
  spatial_rho = c(0.3, 0.7), ar1 = c(0, 0.5),
  trend_strength = c(0, 0.05),
  constants = list(nrow = 20, ncol = 20, n_time = 20,
                   constant_block = FALSE), verbose = FALSE)
length(design)

}
\seealso{
\code{\link[=sim_trend_stack]{sim_trend_stack()}}, \code{\link[=benchmark_methods]{benchmark_methods()}}

Other validation functions:
\code{\link[=benchmark_methods]{benchmark_methods()}},
\code{\link[=benchmark_summary]{benchmark_summary()}},
\code{\link[=compare_detections]{compare_detections()}},
\code{\link[=plot_detection_comparison]{plot_detection_comparison()}}
}
\concept{validation functions}
