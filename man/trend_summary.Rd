% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend_test.R
\name{trend_summary}
\alias{trend_summary}
\title{Summarise CMK trend significance}
\usage{
trend_summary(trend, alpha = c(0.1, 0.05, 0.01), path = NULL, verbose = TRUE)
}
\arguments{
\item{trend}{The \verb{$stats} field of \code{\link[=trend_test]{trend_test()}}'s
output (a 3-layer \code{SpatRaster} with \code{p} and \code{Sm}/\code{S}).}

\item{alpha}{Numeric vector of significance thresholds to report.
\strong{Uncorrected} for multiple testing across cells -- run
\code{\link[=fdr_correction]{fdr_correction()}} on \code{trend$p} before treating any of this as a
final significance result (see \code{?trend_test}, section
"Warning"). The returned table has one row per value in \code{alpha}; the
printed increase/decrease/no-change message uses a single reference
threshold from among them -- \code{0.05} if present (the default vector
includes it), otherwise the strictest (smallest) value supplied.
These three default values are not interchangeable: \code{0.05} is the
conventional standard; \code{0.1} is a more liberal threshold not unusual
in exploratory trend studies; \code{0.01} is markedly more conservative.}

\item{path}{Character or \code{NULL}. If supplied, write the summary table
to this CSV path.}

\item{verbose}{Logical. Print the narrative messages (cell count,
increase/decrease/no-change breakdown). Default \code{TRUE}. Set to
\code{FALSE} to get the returned table silently -- used internally by
\code{\link[=workflow_tst]{workflow_tst()}}/\code{\link[=workflow_rta]{workflow_rta()}} when they call this function only
to populate their
own \code{trend_summary_table} field, independent of their own \code{report}
argument (which already governs whether \code{\link[=trend_test]{trend_test()}}
printed this same summary once, earlier in the same call).}
}
\value{
Invisibly, a data frame with one row per \code{alpha}.
}
\description{
\strong{Function type:} \strong{Reporting/derived function} -- summarises or
plots the output of
another function; it does not compute any new statistic. Not
exported -- called internally by \code{report = TRUE}, and reachable
from outside the package via \code{summary()}.
}
\examples{
r <- read_ordered_stack(example_data("vhp_ndvi"))
trend <- trend_test(r, report = FALSE, verbose = FALSE)

# A table with one row per alpha threshold, plus a printed one-line
# summary at the "reference" threshold (0.05 by default -- see the
# alpha argument above). Called internally by summary() on a
# trend_test() result -- the public entry point is:
summary(trend)

}
\references{
\itemize{
\item Neeti, N. and Eastman, J.R. (2011) A Contextual Mann-Kendall Approach
for the Assessment of Trend Significance in Image Time Series.
Transactions in GIS, 15(5), 599-611. \doi{10.1111/j.1467-9671.2011.01280.x}
}
}
\seealso{
Other Contextual Mann-Kendall functions:
\code{\link[=prepare_cmk_neighbourhood]{prepare_cmk_neighbourhood()}},
\code{\link[=trend_histograms]{trend_histograms()}},
\code{\link[=trend_maps]{trend_maps()}}
}
\concept{Contextual Mann-Kendall functions}
\keyword{internal}
