% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{plot_detection_comparison}
\alias{plot_detection_comparison}
\title{Bar plot of a compare_detections() comparison}
\usage{
plot_detection_comparison(
  comparison,
  metrics = c("Sensitivity", "Specificity", "Precision", "F1", "FPR", "FDR"),
  path = NULL
)
}
\arguments{
\item{comparison}{Output of \code{\link[=compare_detections]{compare_detections()}}, or a data frame
with the same plain (non-suffixed) metric column names -- e.g. the
mean-columns view \code{\link[=compare_detections]{compare_detections()}}'s own \code{replicates = TRUE}
plot dispatch constructs internally.}

\item{metrics}{Character vector of column names in \code{comparison} to
plot. Default: the six proportion-based metrics from
\code{\link[=compare_detections]{compare_detections()}} (\code{TP}/\code{FP}/\code{TN}/\code{FN} are counts on a different
scale and are not plotted by default).}

\item{path}{Character or \code{NULL}. If supplied, a PNG is written there.}
}
\value{
\code{NULL}, invisibly.
}
\description{
A grouped bar plot of the metric columns from a \code{\link[=compare_detections]{compare_detections()}}
result (either a single run, or the mean columns of a \code{replicates = TRUE} aggregated result), one group of bars per method.
}
\details{
\strong{Function type:} \strong{Reporting/derived function} -- summarises or
plots the output of
another function; it does not compute any new statistic. Not
exported -- reachable from outside the package via \code{plot()}.
}
\examples{
sim <- sim_trend_stack(nrow = 12, ncol = 12, n_time = 12, seed = 1)
trend_mk  <- trend_test(sim$series, method = "MK",
                         report = FALSE, verbose = FALSE)
trend_cmk <- trend_test(sim$series, method = "CMK",
                         report = FALSE, verbose = FALSE)
comparison <- compare_detections(
  detections = list(MK = trend_mk$stats$p <= 0.05,
                     CMK = trend_cmk$stats$p <= 0.05),
  ground_truth = sim$true_slope
)

# A grouped bar chart of the table above -- one group of bars per
# method, one bar per metric.
plot_detection_comparison(comparison)

}
\seealso{
Other validation functions:
\code{\link[=benchmark_methods]{benchmark_methods()}},
\code{\link[=benchmark_summary]{benchmark_summary()}},
\code{\link[=compare_detections]{compare_detections()}},
\code{\link[=simulation_design]{simulation_design()}}
}
\concept{validation functions}
