% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slope_estimator.R
\name{slope_histogram}
\alias{slope_histogram}
\title{Histogram of a Theil-Sen slope raster's own values}
\usage{
slope_histogram(slope, breaks = 40, ...)
}
\arguments{
\item{slope}{A \code{SpatRaster} of Theil-Sen slopes, e.g.
\code{slope_estimator()}'s own \verb{$slope}.}

\item{breaks}{Passed to \code{\link[graphics:hist]{graphics::hist()}}'s own \code{breaks} argument.}

\item{...}{Passed on to \code{\link[graphics:hist]{graphics::hist()}}.}
}
\value{
Invisibly, the \code{histogram} object \code{\link[graphics:hist]{graphics::hist()}} itself
returns.
}
\description{
A histogram (with a kernel density overlay) of the raw, continuous
slope values across every valid cell -- the distribution the
diverging map (\code{\link[=slope_map]{slope_map()}}) shows spatially, seen instead as a
single one-dimensional summary.
}
\details{
\strong{Function type:} \strong{Reporting/derived function} -- summarises or
plots the output of
another function; it does not compute any new statistic. Not
exported -- called internally by \code{report = TRUE}, and reachable
from outside the package via \code{plot()}.
}
\examples{
r <- read_ordered_stack(example_data("vhp_ndvi"))
slope <- slope_estimator(r, report = FALSE, verbose = FALSE)
plot(slope, which = "histogram")

}
\seealso{
Other Theil-Sen slope functions:
\code{\link[=slope_direction_barplot]{slope_direction_barplot()}},
\code{\link[=slope_direction_map]{slope_direction_map()}}
}
\concept{Theil-Sen slope functions}
\keyword{internal}
