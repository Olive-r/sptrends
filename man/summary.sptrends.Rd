% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sptrends-methods.R
\name{summary.sptrends}
\alias{summary.sptrends}
\title{Summarise a sptrends result}
\usage{
\method{summary}{sptrends}(object, ...)
}
\arguments{
\item{object}{An object of class \code{"sptrends"} (also one of \code{"tst"},
\code{"rta"}, \code{"workflow_trends"}, \code{"trend_test"}, \code{"slope"},
\code{"prewhiten"}, \code{"fdr"},
\code{"spatial_autocorrelation"}, \code{"compare_detections"},
\code{"sptrends_simulation"}, \code{"sptrends_simulation_design"}, or
\code{"sptrends_benchmark"}),
from \code{\link[=workflow_tst]{workflow_tst()}}, \code{\link[=workflow_rta]{workflow_rta()}}, \code{\link[=workflow_trends]{workflow_trends()}},
\code{\link[=trend_test]{trend_test()}},
\code{\link[=slope_estimator]{slope_estimator()}}, \code{\link[=prewhiten]{prewhiten()}}, \code{\link[=fdr_correction]{fdr_correction()}},
\code{\link[=spatial_autocorrelation]{spatial_autocorrelation()}}, \code{\link[=compare_detections]{compare_detections()}},
\code{\link[=sim_trend_stack]{sim_trend_stack()}}, \code{\link[=simulation_design]{simulation_design()}}, or \code{\link[=benchmark_methods]{benchmark_methods()}}.}

\item{...}{Passed on to the underlying reporting function (e.g.
\code{path}; \code{alpha} for \code{"tst"}/\code{"rta"}/\code{"trend_test"} objects).}
}
\value{
Invisibly, whatever the underlying reporting function itself
returns -- see each section below.
}
\description{
The detailed textual report behind \code{print()}'s own one-line
overview -- see \code{\link[=print.sptrends]{print.sptrends()}} for the full class list and the
rationale for one shared entry point per generic. Each class here
calls its own underlying reporting function directly, listed below,
rather than duplicating what \code{print()} already shows.
}
\details{
\strong{Function type:} \strong{Reporting/derived function} -- summarises an existing
result and does not recompute its statistical analysis.
}
\section{Typical use}{

\verb{result <- workflow_tst(x); summary(result)} for a detailed textual report.
}

\section{Methodological details}{

\strong{Published workflow: \code{"tst"}.}
The full detail behind the \code{"tst"} case of \code{\link[=print.sptrends]{print.sptrends()}}: the
uncorrected trend summary table and, if FDR correction was run, the
FDR summary. Returns a list with \code{trend} and \code{fdr} (or \code{NULL}).
\strong{Published workflow: \code{"rta"}.}
The full detail behind the \code{"rta"} case of \code{\link[=print.sptrends]{print.sptrends()}}: the
uncorrected trend summary table, the Theil-Sen slope summary, and the
FDR-BH summary. Returns a list with \code{trend} and \code{fdr}.
\strong{Configurable workflow: \code{"workflow_trends"}.}
The uncorrected trend table, optional slope summary and selected FDR
summary.
\strong{Trend estimation: \code{"trend_test"}.}
Cell counts and increase/decrease/no-change breakdown at multiple
alpha levels; calls \code{trend_summary()} internally.
\strong{Trend estimation: \code{"slope"}.}
Valid cells, range, median, mean, and the increasing/decreasing/flat
breakdown; calls \code{slope_summary()} internally.
\strong{Diagnostic: \code{"prewhiten"}.}
Valid cells, cells prewhitened, mean rho among them, and median
Durbin-Watson; calls \code{prewhiten_summary()} internally.
\strong{Diagnostic: \code{"fdr"}.}
Significant/not-significant counts and percentages for every method
requested; calls \code{fdr_summary()} internally.
\strong{Diagnostic: \code{"spatial_autocorrelation"}.}
Global results report the observed statistic, permutation distribution
and empirical summary statistics. Local results report the statistic
range, minimum permutation p-value and exploratory raw-significance
count.
\strong{Validation: \code{"compare_detections"}.}
Which method scores best on each numeric metric in the table -- a
small table of its own, \code{metric}/\code{best_method}, not part of what
\code{compare_detections()} itself computes.
\strong{Simulation and benchmarking.}
Simulation summaries quantify the true signal, true-null proportion and
slope range. Design summaries count levels per factor. Benchmark summaries
retain scenario factors and aggregate performance over independent Monte
Carlo replicates, including empirical FDR and FWER where available.
}

\examples{
\donttest{
# Annual mean NDVI from the bundled environmental dataset.
r <- read_ordered_stack(example_data("vhp_ndvi"))
result <- workflow_tst(r, report = FALSE, verbose = FALSE)
summary(result)  # dispatches to the "tst" case above
}

}
\seealso{
\code{\link[=print.sptrends]{print.sptrends()}} for a concise overview and
\code{\link[=plot.sptrends]{plot.sptrends()}} for graphical exploration.
}
