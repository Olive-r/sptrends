% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rta.R
\name{workflow_rta}
\alias{workflow_rta}
\title{Robust Trend Analysis (RTA): the full pipeline in one call}
\usage{
workflow_rta(
  x,
  cmk_args = list(),
  theil_sen_args = list(),
  alpha = 0.05,
  q = 0.05,
  report = TRUE,
  verbose = TRUE,
  n_cores = 1
)
}
\arguments{
\item{x}{A \code{terra::SpatRaster}; each layer is one time step, in
increasing chronological order. Used directly, unprewhitened -- see
"Comparison with TST" under "Methodological details" below.}

\item{cmk_args}{A named list of extra arguments passed to
\code{\link[=trend_test]{trend_test()}} (e.g. \code{list(window_size = 5L)} for a broader CMK
region, or \code{list(method = "MK", n_cores = 4)}). The default empty
list preserves the 3 by 3 CMK region described by Neeti and Eastman
(2011), as implemented in TerrSet's Kendall module;
changing it creates an RTA-inspired variant rather than an exact
reproduction of the published workflow.}

\item{theil_sen_args}{A named list of extra arguments passed to
\code{\link[=slope_estimator]{slope_estimator()}} (e.g. \code{list(max_pairs = 20000, n_cores = 4)}).
\code{smooth_neighbourhood} is left at \code{\link[=slope_estimator]{slope_estimator()}}'s own default
(\code{FALSE}, same as \code{\link[=workflow_tst]{workflow_tst()}}) unless you set it yourself -- see
"Computational considerations" above.}

\item{alpha}{Numeric vector of significance thresholds used for
reporting the (uncorrected) trend result -- passed to
\code{\link[=trend_summary]{trend_summary()}} as \code{alpha}, and used as the single threshold for
\code{\link[=trend_maps]{trend_maps()}}: \code{0.05} if it is one of the values in \code{alpha} (the
default vector includes it), otherwise the strictest (smallest)
value supplied. The three default values are not interchangeable:
\code{0.05} is the conventional standard and the one actually used for
the map; \code{0.1} is a more liberal threshold not unusual in
exploratory trend studies; \code{0.01} is markedly more conservative.
All three are shown side by side in \code{trend_summary_table} for
context, not as equally valid choices.}

\item{q}{Numeric. Target FDR level, passed to \code{\link[=fdr_correction]{fdr_correction()}} --
not an adjusted or renamed \code{alpha}: it limits the expected false
discovery proportion among rejected hypotheses. See \code{\link[=fdr_correction]{fdr_correction()}}
for the full distinction.}

\item{report}{Logical. If \code{TRUE} (default), each step prints its own
summary table and draws its diagnostic plots as it runs (same
effect as calling each function directly with \code{report = TRUE}).
Set to \code{FALSE} for silent, plot-free programmatic use.}

\item{verbose}{Logical. Print progress messages and elapsed time for
the complete workflow. Per-stage times are also returned in \code{timing}.}

\item{n_cores}{Integer. \code{1} (default): every step runs sequentially.
\verb{> 1}: builds \strong{one} \code{parallel::makeCluster()} PSOCK cluster,
shared across every parallel step below (currently the CMK and
Theil-Sen steps) instead of each step creating and tearing down
its own -- avoiding the repeated process-spawn overhead of doing
that two or three times in a row for what is, from the caller's
own perspective, one parallel request. The cluster is closed
automatically when \code{workflow_rta()} returns. Setting \code{n_cores}
inside \code{cmk_args}/\code{theil_sen_args} directly still works exactly as
before (each step falls back to building its own cluster from
that value) -- but only when this top-level \code{n_cores} is left at
its default of \code{1}; when both are set, this one wins and the
per-step \code{n_cores} inside \code{cmk_args}/\code{theil_sen_args} is ignored,
since a single shared cluster and per-step separate ones cannot
both apply to the same call. Mirrors \code{\link[=workflow_tst]{workflow_tst()}}'s own
identical \code{n_cores} argument exactly.}
}
\value{
An object of class \code{c("rta", "sptrends")} (the second, shared
with \code{\link[=workflow_tst]{workflow_tst()}}'s own return value, is for \code{print()}/\code{summary()}/
\code{plot()} -- see \code{\link[=print.sptrends]{print.sptrends()}}): a list
with
\item{theil_sen}{The output of \code{\link[=slope_estimator]{slope_estimator()}}. Not smoothed
by default -- check \code{theil_sen_smoothed} if you need to know
whether it was (e.g. because you set it yourself).}
\item{theil_sen_smoothed}{Logical: whether \code{theil_sen} was computed
with \code{smooth_neighbourhood = TRUE}. \code{FALSE} by default.}
\item{trend}{The output of \code{\link[=trend_test]{trend_test()}}.}
\item{trend_summary_table}{The output of \code{\link[=trend_summary]{trend_summary()}}
(invisible data frame).}
\item{fdr}{The output of \code{\link[=fdr_correction]{fdr_correction()}}, \code{method = "BH"} only.}
\item{timing}{A named list of elapsed seconds per step
(\code{theil_sen}, \code{cmk}, \code{fdr}), each measured with \code{Sys.time()}
around that step's own function call. Coarse (whole-step, not
line-by-line) and not a substitute for a proper profiler, but
enough for noticing which step dominates on your own data.}

Use \code{\link[=print.sptrends]{print.sptrends()}} for a one-line summary.
}
\description{
Implements the complete Robust Trend Analysis workflow for monotonic
trends in gridded raster time series. One of sptrends' two entry
points: chains \code{\link[=slope_estimator]{slope_estimator()}},
\code{\link[=trend_test]{trend_test()}}, and \code{\link[=fdr_correction]{fdr_correction()}} (\code{method = "BH"}
only), in that order, into the \strong{Robust Trend Analysis (RTA)}
workflow. Each step's underlying parameters remain available
directly on the individual functions -- \code{workflow_rta()} does not
replace them, it saves wiring the calls together for the common
case, and returns a single
\code{"rta"} object recognised by \code{\link[=print.sptrends]{print.sptrends()}}. RTA is a
\strong{different, shorter} published workflow from this package's other
integrated pipeline, \code{\link[=workflow_tst]{workflow_tst()}} -- see "Methodological comparison
with TST" below before choosing between them. Both are implemented
here because both are genuinely published methods, not because one
supersedes the other -- this package aims to be a platform for
comparing such methods (see \code{\link[=compare_detections]{compare_detections()}}), not a vehicle
for only its own authors' preferred one.
}
\details{
\strong{When should I choose RTA?}: want to reproduce the published 2024
workflow exactly -> use \code{workflow_rta()}. Want the more recent
workflow, including selective prewhitening and the adaptive BKY
correction -> use \code{\link[=workflow_tst]{workflow_tst()}}. See "Methodological comparison with
TST" below for the reasoning behind each design choice.

Moran's I (\code{\link[=spatial_autocorrelation]{spatial_autocorrelation()}}) is \strong{not} part of this
pipeline, for the same reason it is not part of \code{\link[=workflow_tst]{workflow_tst()}}: it is a
separate diagnostic for the spatial dependence assumption behind
FDR-BH, meant to be run independently (before or after) rather than
chained automatically, and pulling it in here would add a dependency
this function does not otherwise need -- see the package vignette.

\strong{Function type:} \strong{Core function} -- one of the two integrated published
workflows in sptrends.
}
\section{Typical use}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{raster time series
    |
workflow_rta()
    |
Theil-Sen slope + CMK significance + FDR-BH
    |
one `rta` result containing every stage
}\if{html}{\out{</div>}}

RTA analyses the supplied series without prewhitening. If the input
has a seasonal cycle, first use \code{\link[=compute_anomalies]{compute_anomalies()}} and pass its
\code{anomalies} raster.
}

\section{Methodological details}{

\strong{How it works.}

\if{html}{\out{<div class="sourceCode">}}\preformatted{Input raster
    |
slope_estimator    -- how fast? (Theil-Sen)
    |
trend_test         -- is there a monotonic trend? (CMK)
    |
FDR-BH correction  -- which cells survive multiple testing?
    |
"rta" object
}\if{html}{\out{</div>}}

All three steps always run -- unlike \code{\link[=workflow_tst]{workflow_tst()}}, none of them
is optional here, matching the published RTA method exactly (see
"Comparison with TST" under "Methodological details" for what is
deliberately
different between the two workflows). Each step's own output is
kept in full on the returned object (see "Value" below) --
nothing is discarded once a later step begins.

\strong{Statistical assumptions: monotonicity and seasonality.}
Every step here (the Contextual Mann-Kendall test, Theil-Sen) is
designed around a \strong{monotonic} trend -- a consistent tendency to
increase or decrease -- not a periodic/seasonal cycle. If \code{x} has a
seasonal cycle (e.g. raw monthly data with an annual signal), remove
it first with \code{\link[=compute_anomalies]{compute_anomalies()}} and pass the anomalies to
\code{workflow_rta()},
not the raw seasonal series.

\strong{Computational considerations.}
Unlike the Mann-Kendall S statistic, the Theil-Sen slope needs every
pairwise slope in the series (\code{n*(n-1)/2} per cell) to take their
median -- it cannot be accumulated as a running sum, so it does not
scale the same way. For short series (a few dozen time steps) this is
unnoticeable; for long series (hundreds to thousands of steps, e.g.
multi-decade monthly data) it can become the slowest step in the whole
workflow. Tune \code{theil_sen_args} (\code{max_pairs} to subsample pairs,
\code{n_cores} to parallelise) -- see \code{\link[=slope_estimator]{slope_estimator()}}. \code{smooth_neighbourhood}
is left at \code{\link[=slope_estimator]{slope_estimator()}}'s own default (\code{FALSE}) unless you set
it yourself -- neither RTA nor TST originally included any such
smoothing (it is this package's own optional addition on top of both
published methods), so \code{\link[=workflow_tst]{workflow_tst()}} does not default to it
either, for the
same reason.

\strong{Statistical assumptions: \code{alpha} and \code{q}.}
This is one of the most common mistakes in gridded trend analysis, so
it is worth spelling out: \code{alpha} (e.g. \code{0.05}) is defined for a
\emph{single} hypothesis test. A raster is not one test -- it is one test
per cell, potentially thousands of them run simultaneously. Applying
\code{alpha} cell by cell, as if each cell were the only test being run, is
exactly the multiple-testing error this package's FDR step exists to
fix (see the "Warning" section of \code{?trend_test}); it is
not a harmless simplification.

It is common practice to set \code{q} equal to \code{alpha} (e.g. both \code{0.05}) for
convenience and comparability between the uncorrected and corrected
results reported here -- this function does not enforce that, \code{alpha}
and \code{q} are independent arguments. But equal values does not mean equal
meaning: \code{alpha} bounds the error rate of each individual cell's test,
while \code{q} bounds the expected proportion of false positives among all
the cells called significant after correction (see \code{\link[=fdr_correction]{fdr_correction()}} for the
full distinction). Do not read \code{trend_summary_table} (based on \code{alpha},
uncorrected) and the FDR results (based on \code{q}) as answering the same
question just because the numbers match. \code{q = 0.05} has little reason
to change: it is the standard target FDR level in the literature this
package builds on, and lowering it (e.g. to \code{0.01}) mainly costs
statistical power rather than offering a meaningfully different
guarantee.

\strong{Comparison with TST.} RTA (Gutiérrez-Hernández & García, 2024) and
TST (Gutiérrez-Hernández & García, 2025; see \code{\link[=workflow_tst]{workflow_tst()}}) share two
pillars -- Theil-Sen and Contextual Mann-Kendall -- but differ in two
deliberate, independent ways that this function keeps faithful to the
published RTA method, rather than silently reusing TST's later choices.

\strong{Difference 1: prewhitening.} TST's first step, selective AR(1)
prewhitening, does not appear in RTA. Whether to prewhiten before a
Mann-Kendall-family test is a genuine, unresolved methodological debate,
not a settled question with one correct answer. Yue & Wang (2002) find
that prewhitening can substantially reduce power, particularly when a
real trend and real autocorrelation coexist: it can remove part of the
trend signal together with the autocorrelation. Bayazit & Önöz (2007)
argue the opposite case: skipping prewhitening when autocorrelation is
genuinely present can inflate the false-positive rate. RTA does not
prewhiten; TST prewhitens selectively, touching only cells whose own
Durbin-Watson statistic crosses a gating threshold (see \code{\link[=prewhiten]{prewhiten()}}),
specifically to limit unnecessary power loss. Neither position is
implemented here as universally correct.

\strong{Difference 2: FDR-BH only, not adaptive BKY.} \code{\link[=workflow_tst]{workflow_tst()}}
defaults to the two-stage adaptive BKY correction, which estimates how
many tested hypotheses are likely genuinely non-null (\eqn{\hat\pi_0})
and relaxes its threshold when substantial real signal is detected; see
\code{\link[=fdr_bky]{fdr_bky()}}. RTA instead uses the original, non-adaptive
Benjamini-Hochberg procedure, which does not estimate \eqn{\pi_0} and is
derived to control FDR under the global null: the least favourable case
in which every tested hypothesis could be truly null. It therefore keeps
a more conservative guarantee that does not relax as more signal is found.
See \code{\link[=fdr_correction]{fdr_correction()}} for the procedure itself.

These differences are independent: RTA's choice on one does not imply or
require its choice on the other. They happen to be the less adaptive
options in this package's two workflows, not because either dictates the
other.

\strong{Limitations.} RTA is intentionally faithful to its published method:
it does not prewhiten, it always estimates a Theil-Sen slope, and it uses
FDR-BH rather than exposing alternative corrections. Use
\code{\link[=workflow_trends]{workflow_trends()}} when those stages or methods need to be configured.

\strong{Quality assurance.} CMK, slope estimation, and FDR correction are
validated independently in their module functions. Integration tests
verify the RTA stage sequence, argument forwarding, shared parallel
resources, timing fields, S3 return structure, reporting, and agreement
between direct module calls and workflow outputs. See \code{?sptrends} for the
complete internal and external quality-assurance strategy.
}

\examples{
\donttest{
# Annual mean NDVI from the bundled environmental dataset.
r <- read_ordered_stack(example_data("vhp_ndvi"))

# Run the full workflow: Theil-Sen -> Contextual Mann-Kendall ->
# FDR-BH (only BH -- see "Comparison with TST" above
# for why this function does not offer BKY as an option).
result <- workflow_rta(r, report = FALSE, verbose = FALSE)

# An "rta" object: printing it gives a one-line-per-step summary
# (the Theil-Sen slope range, the trend test's cell count, and how
# many cells are significant after FDR-BH correction).
result

# The same plot() methods used throughout this package, rather than
# reconstructing either view by hand: which cells are significant
# after FDR-BH, and how fast those cells are changing.
plot(result, which = "significance")
plot(result, which = "slope")
plot(result)
}

}
\references{
Source of this workflow (primary reference -- this pipeline is a
direct implementation of it):
\itemize{
\item Gutiérrez-Hernández, O. and García, L.V. (2024) Robust Trend Analysis
in Environmental Remote Sensing: A Case Study of Cork Oak Forest
Decline. Remote Sensing, 16(20), 3886. \doi{10.3390/rs16203886}
}

Step 1, Theil-Sen slope (see \code{\link[=slope_estimator]{slope_estimator()}} for the full
reference list):
\itemize{
\item Theil, H. (1950) A rank-invariant method of linear and polynomial
regression analysis. Indagationes Mathematicae, 12, 85-91 (Part I;
published in three parts). No DOI available (pre-DOI-era publication).
\item Sen, P.K. (1968) Estimates of the regression coefficient based on
Kendall's tau. Journal of the American Statistical Association, 63,
1379-1389. \doi{10.1080/01621459.1968.10480934}
}

Step 2, Contextual Mann-Kendall (see \code{\link[=trend_test]{trend_test()}} for
the full reference list, including the foundational Mann-Kendall
statistic it builds on):
\itemize{
\item Neeti, N. and Eastman, J.R. (2011) A Contextual Mann-Kendall Approach
for the Assessment of Trend Significance in Image Time Series.
Transactions in GIS, 15(5), 599-611. \doi{10.1111/j.1467-9671.2011.01280.x}
}

Step 3, FDR-BH correction (see \code{\link[=fdr_correction]{fdr_correction()}} for the complete
reference list):
\itemize{
\item Benjamini, Y. and Hochberg, Y. (1995) Controlling the False
Discovery Rate: A Practical and Powerful Approach to Multiple
Testing. Journal of the Royal Statistical Society: Series B, 57,
289-300. \doi{10.1111/j.2517-6161.1995.tb02031.x}
}

On whether to prewhiten before a Mann-Kendall-family trend test (see
"Comparison with TST" under "Methodological details" above):
\itemize{
\item Yue, S. and Wang, C.Y. (2002) Applicability of prewhitening to
eliminate the influence of serial correlation on the Mann-Kendall
test. Water Resources Research, 38(6), 4-1-4-6.
\doi{10.1029/2001WR000861}
\item Bayazit, M. and Önöz, B. (2007) To prewhiten or not to prewhiten in
trend analysis? Hydrological Sciences Journal, 52(4), 611-624.
\doi{10.1623/hysj.52.4.611}
}
}
\seealso{
Other pipeline functions:
\code{\link[=workflow_trends]{workflow_trends()}},
\code{\link[=workflow_tst]{workflow_tst()}}
}
\concept{pipeline functions}
