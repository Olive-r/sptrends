% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slope_estimator.R
\name{slope_direction_map}
\alias{slope_direction_map}
\title{Direction-of-change map for a Theil-Sen slope raster}
\usage{
slope_direction_map(slope, ...)
}
\arguments{
\item{slope}{A \code{SpatRaster} of Theil-Sen slopes, e.g.
\code{slope_estimator()}'s own \verb{$slope}.}

\item{...}{Passed on to \code{\link[terra:plot]{terra::plot()}}.}
}
\value{
Invisibly, a \code{SpatRaster} with the classification (\code{-1}
negative, \code{0} zero, \code{1} positive).
}
\description{
A categorical map classifying every valid cell as \code{"Positive"},
\code{"Negative"}, or exactly \code{"Zero"} -- the binary positive/negative
question a reader usually wants, with the (rare, for continuous
Theil-Sen estimates) exact-zero case shown separately rather than
folded into either side.
}
\details{
\strong{Function type:} \strong{Reporting/derived function} -- summarises or
plots the output of
another function; it does not compute any new statistic. Not
exported -- called internally by \code{report = TRUE}, and reachable
from outside the package via \code{plot()}.
}
\examples{
r <- read_ordered_stack(example_data("vhp_ndvi"))
slope <- slope_estimator(r, report = FALSE, verbose = FALSE)
plot(slope, which = "direction")

}
\seealso{
Other Theil-Sen slope functions:
\code{\link[=slope_direction_barplot]{slope_direction_barplot()}},
\code{\link[=slope_histogram]{slope_histogram()}}
}
\concept{Theil-Sen slope functions}
\keyword{internal}
